# Audit — naming consistency (2026-08-28)

Read-only catalogue on branch `enemy-world-work`; nothing was modified. All
paths are relative to the repo root. Counts are occurrences in `core/ autoload/
data/ ui/ effects/ scenes/ spells/ tools/` (`.gd/.tres/.tscn`) unless marked
"docs".

Persistence facts used for risk ratings (from `autoload/SaveData.gd:4-121`):
saves store `attempt_*` fields (incl. `attempt_pending_big_choice`,
`attempt_big_choice_source_segment`, `attempt_doctrine_*`, `attempt_segment1_*`,
`attempt_vendor_*`, `attempt_inventory`, `attempt_bag`),
`meta_permanent_augment_ids` (augment ids), `meta_seen_manifestation_cards`
(manifestation ids), `meta_discovered_enemy_ids` (enemy spec ids), `meta_stash`
(item def ids + slot indices). Anything in those families is a save-format
rename.

---

## 1. Inconsistency table

| # | Term | Variants found (count; evidence) | Canonical | Player-visible? | Action | Risk |
|---|---|---|---|---|---|---|
| 1 | **Armor / Armour** | Code: `armor` 75 (slot id `&"armor"` `data/items/Inventory.gd:16`; `Stats.armor`; `armor_add` in `data/RaceData.gd:10`, `data/styles/StyleData.gd:8`; `ManifestationCatalog.SLOT_ARMOR`), `armour` 25 (`effects/manifestations/logic/ScarTissue.gd:22,40,57` `_armour`, `ARMOUR_CAP`; `tools/tests/BurdenSystemTest.gd:101`). **UI "Armor"** ×10: `Inventory.gd:16` label, `ui/augments/AugmentSelect.gd:393`, `ui/widgets/AugmentTooltip.gd:147`, `core/systems/major_choice/effects/MCE_AddStatDelta.gd:30`, `data/augments/Augment_StaminaCore.tres:21`, `data/items/defs/conduit/conduit_plating.tres:19`, `gravemarch_carapace.tres:20`, `lattice_shellplate.tres:20`, `data/major_choices/major_frame_reinforcement.tres:21`, `data/sets/gravemarch/Gravemarch.tres:63`. **UI "Armour"** ×4: `data/augments/Augment_DoctrineOfBurden.tres:13`, `data/items/defs/curses/curse_ashen_ballast.tres:21-22`, `data/manifestations/ManifestationCatalog.gd:155`, `ScarTissue.gd:110`. Docs: roadmap itself uses both ("Armour" §5.2 slot list & §9 "Armoured"; "Armor" §5.4 lines 274,286); design docs mostly "Armour" (`docs/design/MANIFESTATIONS.md:23,159`, `NEG_VERTICAL_SLICE.md:56`) but `NEG_BUILDCRAFT_ARCHETYPES.md:58,219`, `RARITY_MERGE_SPEC.md:99` "Armor". | Code: `armor` (dominant, persisted slot id + resource field). Prose: undecided; rest of narrative is British (see #2) | **Yes** | UNIFY-UI (pick one, 4 vs 10 strings) + UNIFY-DOC (roadmap contradicts itself). KEEP code identifiers. | UI low. Code rename HIGH: `&"armor"` slot id and `armor_add` are resource/save fields. |
| 2 | **British vs American spelling (player text)** | UK: "recognises/recognised/recognise" `data/narrative/OpeningSequenceData.gd:25,61`, `data/narrative/Segment1Text.gd:29,36,46`; "authorised/authorisation" `OpeningSequenceData.gd:43,45`, `Segment1Text.gd:35,55`; "favour" `Augment_InversionLens.tres:10`; "DESTABILISED" `Segment1Text.gd:47`; "Stabilise" `ui/screens/opening/OpeningPresentation.gd:79`; "Travelling" `ManifestationCatalog.gd:59`; "cancelled" `ui/screens/settings/SettingsScreen.gd:163`; "colour" `data/manifestations/ManifestationNouns.gd:43-47` (dict keys). US: "recognizes" `data/major_choices/doctrines/apotheosis_law_of_admission.tres:20`; "authorized" `major_relic_crusher_ring.tres:19`; "stabilizes" `major_ritual.tres:18`, `ui/widgets/ItemTooltip.gd:292`; "defense" `Gravemarch.tres:63`; "Armor" (#1). | British (dominant in narrative and manifestation text) | **Yes** | UNIFY-UI (5 US strings) | Low |
| 3 | **POS / NEG / curse / negative / polarity** | `Polarity.POS/NEG` enum (`data/items/ItemInstance.gd`), `pos` 326 / `NEG` 109 / `POS` 90 / `polarity` 89; `curse` 89 / `curses` 38 / `cursed` 12 (the NEG roll's stat effect: folder `data/items/defs/curses/`, ids `curse_*`); `negative` 28 (`negative_effect_scenes` resource field, `core/systems/items/ItemEffectRunner.gd:10`); `blessing` 2. UI: "POS/NEG" `ItemTooltip.gd:160`, `ui/widgets/RunSheetHUD.gd:444`; "curse" `ItemTooltip.gd:254,260,291`, `RunSheetHUD.gd:491,499`, augment blurbs. Docs: NEG 182, cursed 21, negative 9. | Roadmap layers them deliberately: NEG = polarity, curse = its stat roll, Burden = active curse total | Yes (consistent) | KEEP; UNIFY-DOC: one glossary line stating the layering | None |
| 4 | **Burden vs census vs corruption** | `burden` 62, `BurdenResolver`/`BurdenSnapshot`; the census is named `neg_count` (24) — `census` exists only in comments/tests (`core/systems/items/BurdenResolver.gd:54`, `BurdenSnapshot.gd:43`, `BurdenSystemTest.gd:278`); `corruption_total`, `MIN_CORRUPTION_SEVERITY` = Corruption Engine severity. UI: "Burden" `ItemTooltip.gd:254`, "0%% burden" lowercase `RunSheetHUD.gd:491`, "%d NEG / %d POS · %d active · %d%% total severity" `RunSheetHUD.gd:444`. | Roadmap §5.2: "polarity census" vs "active stat Burden" | Yes | KEEP code; UNIFY-UI casing ("Burden") | None |
| 5 | **Doctrine (3 meanings)** | (a) NEG augment `augment_doctrine_of_burden`, tuning statics `BurdenResolver.doctrine_armor_per_item/doctrine_hp_cap` `BurdenResolver.gd:23-26`. (b) "Ascension Doctrine" major-choice system: stages `&"method"/&"doctrine"/&"apotheosis"` `core/systems/major_choice/MajorChoiceDef.gd:81`; ALL stage resources prefixed `doctrine_` incl. `doctrine_method_black_archive`, `doctrine_apotheosis_*` (`data/major_choices/doctrines/*.tres`); persisted `attempt_doctrine_version/stage_ids/rules/events/threat_debt` `SaveData.gd:81-87`; `DoctrineRewardService`, `MCE_AddDoctrineTag`, `MCE_ModDoctrineRule`, `Global.doctrine_healing_multiplier` (`player.gd:1247`, a major-choice hook, not the augment); `ASCENSION_DOCTRINE_VERSION`; tests `AscensionDoctrine*Test`. UI: "ASCENSION DOCTRINE" `ui/screens/MajorChoice.tscn:80`, "DOCTRINE // THE SYSTEM LEARNS TO WORSHIP" `MajorChoice.gd:51`, "DOCTRINE READY / Ascension thesis awaiting inscription" `ui/screens/HubShop.gd:380`, "Examine each thesis" `MajorChoice.tscn:92`. (c) style mutation title "Scatter Doctrine" `data/major_choices/major_ranged_shotgun.tres:15`, `MCE_AddMutation.gd:32`. Also **Major Choice vs Big Choice vs thesis**: `pending_big_choice` 26, `attempt_big_choice_source_segment` 17 (persisted `SaveData.gd:66,70`) vs `major_choice` 51, `attempt_major_choice_id` (`SaveData.gd:73`), "Force Major Choice Pending" `ui/screens/MainMenu.tscn:355`. Docs: doctrine 104, thesis 3, major choice 6, big choice 0. | Roadmap knows only "Doctrine of Burden". No canonical name for the thesis system. | **Yes** | UNIFY-DOC (glossary: "Ascension Doctrine" = thesis system; "Doctrine of Burden" = augment); UNIFY-UI: retitle "Scatter Doctrine" (1 string); UNIFY-CODE optional: `BurdenResolver.doctrine_*` → `burden_doctrine_*` (not persisted; tests read `doctrine_bonus` dict keys `BurdenSystemTest.gd:315-327`). KEEP `attempt_doctrine_*`, `*big_choice*`. | UI/doc low. Persisted keys HIGH. |
| 6 | **attempt vs run vs Ascension** | Code: `attempt_*` (61+ families, all persisted `SaveData.gd:53-121`), `start_new_attempt` 23; `run_luck` 39, `run_sheet` 34, `total_runs` (`SaveData.gd:18`). UI "attempt": "NO ACTIVE ATTEMPT"/"ATTEMPT ACTIVE" `ui/components/SaveCard.gd:173,205`; "(this attempt)" `major_frame_reinforcement.tres:21`, `major_resonant_focus.tres:21`, `major_satchel.tres:15`, `major_augment_overclock.tres:27`. UI "run": "Start Run" `ui/screens/base.tscn:255`, "SAVES / NEW RUN" `MainMenu.tscn:203`, "RUN SHEET // ACTIVE VESSEL" `RunSheetHUD.tscn:49`, "The run records…" `MajorChoiceCard.tscn:131`, "next new run" `base.tscn:226`. UI "Ascension": "Death would end the Ascension" `HubShop.gd:1120`, "This Ascension ends here"/"ASCENSION FAILED" `scenes/game.gd:385-386`. Docs: run 584, attempt 13. | "run" (roadmap) | **Yes** | UNIFY-UI: attempt → run in 6 strings; keep "Ascension" as the death/finality flavour. KEEP code. | UI low. Code HIGH (save schema). |
| 7 | **Manifestation vs rule vs engine vs perk vs protocol vs Manifest** | Code: `rule` 178 / `rules` 47 vs `manifestation*` ≈100; `ManifestBadge` (`ui/components/ManifestBadge.gd:2`, callers `ui/bag/BagSlot.gd:33`, `ui/components/InventorySlotView.gd:28`). UI: "Manifestation" `SettingsScreen.gd:292`, `HudManifestationController.gd:402`, `core/systems/world/CursedVault.gd:78`; "rule" in player text `HudManifestationController.gd:402,404,407,409`, `ItemTooltip.gd:203` ("never reroll its rule"); "engine" = combination `HudManifestationController.gd:360`; "protocol" `ui/controllers/ManifestationPairNotifier.gd:163` ("Full protocol recorded in Run Sheet"); dev "Granted rule"/"Clear rules" `PerformanceOverlay.gd:960-965`. Docs: rule 127, manifestation 85, `MANIFESTATIONS.md` table header "Rule". | Roadmap §13: "Manifestations — engines, not perks" (Manifestation = thing, engine = combination) | **Yes** | KEEP Manifestation/engine; UNIFY-UI: "rule" is defined once at `:402` so later uses are defensible, but "protocol" (`:163`) is a fourth word — replace. UNIFY-CODE optional `ManifestBadge` → `ManifestationBadge` (class + node-name lookup `ManifestBadge.gd:23`). | Low |
| 8 | **pair vs bond vs synergy** | `pair` 126 (`ManifestationPairDef/Effect/Catalog/Notifier`); `bond_multiplier`, `BOND_PER_TAG`, `BOND_DUO_BONUS` `data/manifestations/ManifestationCatalog.gd:280-316` (= roll-weight bias toward held nouns, a different concept); `synergy` 1 comment. UI never says "pair" except dev overlay `PerformanceOverlay.gd:974`. Docs: pair 14, bond 0. | pair | Marginal | KEEP (bond is a distinct mechanic); UNIFY-DOC define "pair" and "bond" | None |
| 9 | **Luck vs Fortune vs Misfortune vs Lucky Crit** | `luck` stat (slot `&"luck"`, `run_luck`, `LuckResolver`); `fortune` = noun (`ManifestationNouns.gd:47` label "FORTUNE"); `misfortune` = Broken Providence bank; "Bad Fortune Engine" pair. Casing: "Lucky Crit(s)" 34 vs "lucky crit(s)" 7 (`Augment_LuckyCharm.tres:18`, `ui/widgets/...`). | Roadmap lists both Luck (axis, §17) and Fortune (noun, §14) | Yes | KEEP; UNIFY-UI capitalisation of "Lucky Crit" | None |
| 10 | **Exit Rite vs Gate vs Rite vs Evac vs Escape** (+ state words) | Code: `core/systems/world/ExitRite.gd`, `scenes/world/gates/ExitRite.tscn` (folder "gates"), group `&"exit_rite"`, signal `gate_unsealed` 22, `RunEvents.gate_checklist_changed` `autoload/RunEvents.gd:92`, `HudGateOverlayController`, `HudGateChecklistController`, `GateOverlay.tscn`, `GateReadyOverlay`, `_gate_arrow` 26, `tip_shown_gate_hold`; `EvacOverlay`/`HudEvacOverlayController`, `evac_pressure` 6, `evac_time_base_sec`; `ThreatDirector.rite_channel_changed` `:163`; `RiteProgressLedger`/`RitePulseResolver`. UI: "EXIT RITE • LOCKED" `ui/overlays/GateOverlay.tscn:328`, "RITE EXPOSED" `:168`, "Rewrite the outer Rite" `:177`; "The rite rejects you" `ExitRite.gd:497`; "The Rite draws for %ds" `:509`; "Rewrite the Rite • Remain within the sigil" `:521` (narrative mode) **vs** "Hold within the Gate to Escape" `:523` (proc segments); "reveal the gate marker"/"follow the orange gate marker" `SegmentProcBuilder.gd:594,602`; "GATE UNSEALED • EVAC IN %ds"/"EVAC NOW!" `HudEvacOverlayController.gd:124,130`; HUD "SEALED"/"READY" `ui/screens/HUD.tscn:360`, `HudGateOverlayController.gd:222`; checklist state `"locked"` `HudGateChecklistController.gd:17`; "OUTER RITE DESTABILISED" `Segment1Text.gd:47`; "Exit Rite" in doctrine text (`major_ritual.tres:18`, `doctrine_pilgrim_engine.tres:33`, `doctrine_vessel_without_mercy.tres:29,31`). Docs: exit rite 63, rite 163, gate 76 (mostly plan docs: `docs/superpowers/plans/2026-08-27-phase2-one-great-run.md:39,158`, `docs/gamegoal.md:336`), evac 3. | "Exit Rite" (roadmap §7, §18) | **Yes** — same object called Gate/Rite/Exit Rite; closed state is LOCKED/SEALED/locked/EXPOSED/DESTABILISED; open is UNSEALED/READY | UNIFY-UI: "Gate" → "Rite" in `ExitRite.gd:523`, `SegmentProcBuilder.gd:594,602`, `HudEvacOverlayController.gd:124`; choose SEALED or LOCKED. KEEP code names (`gate_*` signals wired across HUD + tests). UNIFY-DOC in plan docs. | UI low. Code medium (signal names, ~50 sites, not persisted). |
| 11 | **Wardstone verb: Attune vs Rewrite vs activated; respawn/checkpoint/reconstruction** | "Wardstone Attuned: new respawn anchor" `core/systems/world/Wardstone.gd:133`, "Stand within the Wardstone to Attune" `:201`, `attune_time_sec`, `tip_shown_wardstone_attune` **vs** "Rewrite the first Wardstone" `Segment1Text.gd:15`, "WARDSTONE REWRITTEN" `:36`, "Archive Wardstone rewritten"/"Next: rewrite the archive Wardstone" `Level1Builder.gd:1661,1674`, "last rewritten Wardstone" `scenes/game.gd:379` (both fire in Segment 1); `_on_wardstone_activated` (code); "Attune relay nodes" `DistrictRelayObjective.gd:75` reuses "attune" for another object. Death: UI "reconstruction cost" `ui/screens/hud.gd:544`, `HubShop.gd:1120` vs code `compute_respawn_cost`, `attempt_checkpoint_pos` (persisted), reason `&"reconstruction"` `global.gd:1928`. | Roadmap: "Wardstone" only. In-fiction verb "Rewrite" matches "Rewrite the Rite". | **Yes** | UNIFY-UI: pick Rewrite or Attune (2 strings in `Wardstone.gd`). KEEP code. | Low |
| 12 | **Segment vs District vs Area vs Level** | Code: `attempt_segment` (persisted) 92, `SegmentProcBuilder`, `SegmentPlan`, `Segment1Text`, `Segment1SpawnProfile` **vs** `Level1Builder` 16, `Level1MilestoneArea`, `Level1DeterminismProbe`, node `&"Level1"` `scenes/game.gd:36`, `level1_builder` 8, `Level1_Geometry` **vs** `DistrictPlan`, `ChunkGenDistrict`, chunk archetype `&"district"`. UI: "AREA 1 · SEGMENT %d" `SaveCard.gd:173`, "Area 1 · Segment 1"/"Continue to Segment %d" `HubShop.gd:384,394`, "SEGMENT REPORT" `HubShop.tscn:169`, "Once per segment" `apotheosis_manufactured_witness.tres:19` vs "in every district" same file `:15`; "District heat" `HudThreatController.gd:225`; "District boss defeated"/"defeat the district boss" `SegmentProcBuilder.gd:590,600`; "signals detected in this district" `:142-144`; "THE DISTRICT SHIFTS — …" `EncounterDirector.gd:226`; "the district notices" `method_black_archive.tres:36`. No `area` concept exists in code. Docs: segment 209, district 10, level1 4. | Segment (roadmap; "districts" only as procedural spaces in Tier 2) | **Yes** | UNIFY-UI: decide whether "district" is the fiction name of a segment and use it consistently, or replace 5 strings; drop/define "Area 1". UNIFY-CODE optional `Level1*` → `Segment1*` (scene node name + `Level1_Geometry` + tests; not persisted). | UI low. Code medium. |
| 13 | **Threat vs heat vs pressure vs Overtime vs Resonance** | `threat` 44 (HUD number), `heat`/`pressure_heat` 33/7 (internal curve), `pressure` 95 — split between gameplay (`ThreatDirector.evac_pressure`, `add_overtime_pressure`) and performance load-shedding (`EnemySimulationScheduler.set_physics_pressure_override` 35, `_pressure_level` 21, `&"severe_pressure_engaged"`), `overtime` 23, `resonance` 80, `escalation` 13. (`wanted` 65 is a false positive: local vars in Settings/Sfx.) UI: "THREAT"/"T%d %.1f" `HUD.tscn:371,386`; "District heat: %d%%" `HudThreatController.gd:225`; "OVERTIME ×%.1f" `:227`; "Loot rarity +%d from the danger" `:229`; "Threat: %s" `EnemyDossierCatalog.gd:37`; "adds 25 Threat" `apotheosis_manufactured_witness.tres:20`; "RESONANCE" `HUD.tscn:345` vs "reach %d%% resonance" `SegmentProcBuilder.gd:594`. Docs: pressure 134, threat 73, heat 20 (`docs/current_game_data.md:422-455`), resonance 77, overtime 20, roadmap: Overtime 0. | Threat (player number), Resonance (progress); "pressure" is design vocabulary; "heat" internal | "heat" leaks once | UNIFY-UI: rename "District heat" tooltip line. UNIFY-DOC: roadmap should name Overtime; disambiguate gameplay pressure vs scheduler pressure. UNIFY-CODE optional: `EnemySimulationScheduler` pressure → `load`/`budget` (benchmarks JSON keys in `performance_results/`). | Low / code medium |
| 14 | **ThreatDirector phases** | `&"recon"/&"disturbance"/&"ascension"/&"collapse"` `autoload/ThreatDirector.gd:289` — match roadmap. Two code names for the same setter: `set_segment_phase` (ThreatDirector) / `_set_pressure_phase` (`SegmentProcBuilder.gd:511`) / signal `segment_phase_changed` (`RunEvents.gd:77`). Player sees "THE DISTRICT SHIFTS — ASCENSION" (`EncounterDirector.gd:226`) — "ascension" collides with game title, "Ascension Doctrine", "end the Ascension". | Roadmap ids | Yes (popup) | KEEP ids; UNIFY-CODE trivial `_set_pressure_phase` → `_set_segment_phase` (private, 8 sites). UNIFY-DOC note Ascension overload. | None |
| 15 | **elite / champion / miniboss / boss** | `elite`/`is_elite` 148/70; champion/veteran 0; `miniboss` 18 + `MiniBossArena` (scene/class) + group `&"miniboss"`; docs "minibosses" 12, "miniboss" 5, "MiniBoss" 2, "mini-boss" 0. "tank"/"mage" only as `boss_archetype` meta (`scenes/world/events/BossArena.gd:148`, `BossBulldozerBrain.gd:42`) and "Tank core" blurb `Augment_StaminaCore.tres:20` — never for the Brute. Roadmap §9 modifiers (Armoured/Vampiric/Shielded/Splitting/Fast) do not exist in code (only `elite_hp_mult`/`elite_speed_mult`). | elite; miniboss (one word) | Yes (consistent) | KEEP; UNIFY-DOC: "miniboss" one word; note §9 is unimplemented vocabulary | None |
| 16 | **Follower vs believer vs belief vs support vs witness** | `followers` 125 (persisted `attempt_followers`, `best_followers`); `belief` 18 (`belief_defiance`, `follower_belief_power`); `believer` 4 comments only (`effects/manifestations/pairs/DebtCollector.gd:8`, `TitheRhythm.gd:6,45,62`); `congregation` 4; `witness` (`attempt_witness_used_segment`, `&"witness_expended"`). UI: "Followers" dominant (`HUD.tscn:325`, HubShop, SaveCard); "CURRENT SUPPORT" `HubShop.gd:384`, `HubShop.tscn:176`; "Insufficient support" `HubShop.gd:1111`; "Support is committed elsewhere" `ui/controllers/FollowerFeedbackUI.gd:65`; "Supporters search the city's…" `FollowerFeedbackUI.gd:57` vs "%d Followers search the city's…" `HubShop.gd:401`. Docs: follower 65, believer 0, belief 17. | Followers (roadmap §16; "belief" reserved for future meaning) | **Yes** | UNIFY-UI: "SUPPORT"/"Supporters" → "Followers" (4 strings). KEEP belief flavour. | Low |
| 17 | **Wardstone / ward** | `Wardstone` 29 + `wardstone` 19 + `wardstones` 16; group `&"wardstones"` `Wardstone.gd:40` vs heal source `&"wardstone"` `player.gd:1128`, `global.gd:1034` vs milestones `&"wardstone_1"/_2` `Level1Builder.gd:70,75` vs safeguard `"wardstone:%d"` `SegmentProcBuilder.gd:342` vs `&"segment1:wardstone:1"` `Level1Builder.gd:1142`. "Ward" also = Manifestation noun (`&"ward"` 32) and `WardVigilObjective` ("Ward Vigil"). Docs "ward stone" 0. | Wardstone | Yes (consistent) | KEEP; UNIFY-DOC glossary for the three "ward"s | None |
| 18 | **Resonance vs Overtime** | Distinct and never conflated: Resonance = 0–100 % progress, Overtime = post-unseal multiplier + "Overtime Gospel". | both | Yes | KEEP; UNIFY-DOC (roadmap omits Overtime) | None |
| 19 | **Rarity tiers** | Integer everywhere; UI "R%d" `ItemTooltip.gd:160` vs "r%d" `ui/bag/BagSlot.gd:199`; "rank this item up" `ItemTooltip.gd:203`; "high‑rarity" `major_relic_crusher_ring.tres:19`; "Loot rarity +%d" `HudThreatController.gd:229`; docs R1–R6 (`docs/design/RARITY_MERGE_SPEC.md`), explicitly no named ladder (`docs/gamegoal.md:472`). "tier" = set breakpoint in code (`SetTier`, "%dP" `SetBreakpointNotifier.gd:82`) and threat tier "T%d" in HUD. | numeric R-n | Yes | UNIFY-UI: `r%d` → `R%d` | None |
| 20 | **Equipment slot / stat labels** | Ids `&"hp"/&"armor"/&"move"/&"power"/&"haste"/&"luck"/&"offhand"/&"ring"` `Inventory.gd:15-22`; consts `SLOT_HP…SLOT_RING` `ManifestationCatalog.gd:16-23`. Labels `Inventory.gd`: Health/Armor/Movement/Power/Haste/Luck/Offhand/Ring, hints HP/ARM/**MOV**/**PWR**/HST/LCK/OFF/RING **vs** second hard-coded table `ui/screens/InventoryStash.gd:14` `EQUIP_HINTS = ["HP","ARM","MOVE","POW",…]` (MOVE≠MOV, POW≠PWR). `ItemTooltip.gd:327-332` "HP/…/Movement/Haste/Luck". Stat prose: "Max HP" (`major_frame_reinforcement.tres:21`, `Augment_StaminaCore.tres:21`) vs "maximum HP" (`Gravemarch.tres:18`) vs "Health" (Inventory label, dossier "Health:"); "Move Speed" (`AugmentSelect.gd:396`, `AugmentTooltip.gd:149`, `MCE_AddStatDelta.gd:32`) vs "movement speed" (`Conduit.tres:18`, `Gravemarch.tres:18`, `Lattice.tres:17`, `method_frame_of_ash.tres:30`) vs "Movement" (slot) vs roadmap "Move" vs field `speed_add` (`RaceData.gd:11`). Roadmap says "attack speed" (§12.3) — the game only ever says "Haste". Docs `MANIFESTATIONS.md:23` "Movement" but `:159` "Move". | Roadmap: HP, Armour, Move, Power, Haste, Luck, Offhand, Ring | **Yes** | UNIFY-UI: make `InventoryStash` read `Inventory.slot_definition(i).hint` (deletes the divergent table); one of Move Speed/movement speed; one of Max HP/maximum HP. UNIFY-DOC: "attack speed" → "Haste", "Movement" → "Move". KEEP ids. | UI low; ids HIGH (persisted slot indices/ids) |
| 21 | **Manifestation nouns** | Code: exactly `ward, momentum, fortune, cadence, shard` (`ManifestationNouns.gd:52`), catalog tags use only these (cadence 12, ward 9, momentum 9, fortune 9, shard 8). Label "SHARDS" plural while the other four are singular (`ManifestationNouns.gd:45`). `docs/design/MANIFESTATIONS.md:157-173` "Family" column still lists pre-noun tags: stability, rhythm, charge, mark, lowhp, evade, misfortune, followers, explore, greed. Test uses capitalised `&"Momentum"/&"Shard"/&"Ward"` (`tools/tests/BuildIdentityTest.gd:53,88`) while runtime ids are lowercase (BuildIdentity only formats them, so the test never touches real ids). | Roadmap: Momentum, Shard, Fortune, Ward, Cadence — matches code | Yes | UNIFY-DOC (stale table); fix test ids; decide SHARD vs SHARDS | None |
| 22 | **Encounter beats / grammar** | Code ids+labels: `charger_wedge` "CHARGER WEDGE", `shield_wall` "SHIELD WALL", `sniper_crossfire` "CROSSFIRE", `summoner_nest` "NEST", `hunter` "HUNTER", `bomber_carpet` "BOMBER CARPET", `leech_ring` "LEECH RING" (`core/systems/encounters/EncounterBeats.gd:31-129`). Roadmap §8.1 grammar: Assault, Formation, Hunter, Nest, Convoy, Lockdown, Hunt target, Ritual interference — only Hunter/Nest overlap. Code also mixes `beat` 41 / `wave` (`_run_authored_wave`) / `burst` (`ExitRite.BURST_STAGES`) / `encounter`. | beat (code) vs grammar class (roadmap) | Labels only | UNIFY-DOC: map each beat to a grammar class (or add a `grammar` field) | None |
| 23 | **Cursed Vault** | `CursedVault` class, group `&"cursed_vault"` `CursedVault.gd:34`, drop-source `&"vault"` `:103`, signal `vault_opened`, popups "CURSED VAULT"/"THE VAULT ANSWERS" `:76,127`, tip "Stand in the vault…" `:78`. Docs 7. | Cursed Vault | Yes (consistent) | KEEP | None |
| 24 | **Augment / ability / mutation; set naming** | "Augment" consistent in UI; "Ability" = set active (`ActiveAbilityHUD.tscn:59`), but "Ability Callouts" setting (`SettingsScreen.gd:295`) governs augment + Manifestation callouts; "mutation" = style mutation from a major choice (`MCE_AddMutation.gd:30-36`), absent from roadmap. Set display names differ in form: `&"conduit"` → "Conduit", `&"gravemarch"` → "Gravemarch Protocol", `&"lattice"` → "Lattice Index" (`data/sets/*/*.tres:47,54,54`). Lattice 6-piece has three names: id `lattice_6_index_commit` (`effects/lattice/scenes/LatticeEchoBuffer.gd:49`), display "Triangle Commit" (`Lattice.tres:44`), glossary/description "Index Commit" (`Lattice.tres:45,63`). Gravemarch 6-piece: id `gravemarch_6_verdict` (`GravemarchMassArrest.gd:63`) vs display "Mass Arrest". "breakpoint" (code, `ItemTooltip.gd:500` "No set breakpoint crossed") vs "%dP … pieces" in UI. | — | **Yes** | UNIFY-UI: one form for set names; one name for Lattice 6P; UNIFY-DOC: define "mutation" | Low |
| 25 | **Bag / Backpack / Satchel / Stash / Gear / Inventory / Equipment** | Code: `BagInventory`, `BagUI`, `BagSlot`, `HudBagController`, `attempt_bag` (persisted), `Inventory` (= equipped), `StashInventory`/`InventoryStash`, `MCE_AddBackpackSlots`. UI: "Backpack" `HubShop.tscn:236`, `HubShop.gd:384,1103`; "Gear, Backpack or Stash" `HubShop.gd:959`; "Bag"/"Offer Bag" `HubShop.tscn:424,341`; "Expanded Satchel" + "+4 backpack slots" `major_satchel.tres:14-15`; "Gear"/"GEAR REGISTER" (= equipped) `HubShop.tscn:353,231`; "Manage Inventory"/"Inventory" (= stash screen) `HubShop.tscn:196-197`; "Equipment" `InventoryBar.gd:70`, `Inventory.gd:27`. | none set; Backpack/Gear/Stash is the majority | **Yes** | UNIFY-UI: Backpack (bag), Gear (equipped), Stash; rename "Offer Bag"/"Bag"/"Manage Inventory". KEEP code. | Low |
| 26 | **Exchange / Hub / Shop / Vendor / Trade** | Code `HubShop.gd/.tscn`, `HubShopBackdrop`, `attempt_vendor_*` (persisted), test `ExchangeIdentityTest`. UI: "THE EXCHANGE" `HubShop.tscn:76`, "exchange" ×12, "Undo Last Trade"/"LAST TRADE UNDONE" `HubShop.gd:217,358`, "Vendor stock" `HubShop.gd:959,1351`, "this HUB" `HubShop.gd:219`. | Exchange (UI) | Yes | UNIFY-UI: "HUB" → "Exchange"; Trade vs Exchange pick one. KEEP code. | Low |
| 27 | **Playstyle / Style** | `StyleData`, `CombatStyleTuning`, `PlaystyleCard`, `StyleParityTest`; UI "Playstyle" `base.tscn:135` vs "Selected style:" `base.gd:149` vs "Race • Style" `SaveCard.tscn:187` vs "Style" `MainMenu.tscn:301`. | style | Yes | UNIFY-UI (minor) | None |
| 28 | **Sigil (4 meanings)** | Exit Rite circle (`ExitRite.tscn:16` node "Sigil"; "Remain within the sigil" `ExitRite.gd:521`, `Level1Builder.gd:1672`), Wardstone sprite (`Wardstone.tscn:20`), waypoint markers `WaypointSigil` ("9 sigils"), "Predestination Sigil" (Manifestation), "Tri‑Sigil Weave" (mutation), roadmap "Equilibrium Sigil" (A3). | — | **Yes** | UNIFY-UI: reserve "sigil" for one thing (the Rite circle) | Low |
| 29 | **Mark / mark** | "Mark"/"Marked" capitalised as the Predestination/Pilgrim's Toll mechanic (27) vs lowercase Lattice "marks" (`Lattice.tres:45`) — two unrelated mechanics, same word (`VFX_SigilMark`, `LatticeMarkVfx`, `debug_clear_marks`). | — | Yes | UNIFY-DOC define; consider renaming Lattice marks | Low |
| 30 | **Ascension (title vs phase vs system vs run)** | Game title; phase `&"ascension"` (popup `EncounterDirector.gd:226`); "ASCENSION DOCTRINE" screen; "Death would end the Ascension" (= the run). | — | **Yes** | UNIFY-DOC; see #6, #14 | None |

---

## 2. Ten most player-visible inconsistencies (UI text vs docs/roadmap)

1. **Exit Rite is also "the Gate", and its state has six names.** "Hold within the Gate to Escape" (`core/systems/world/ExitRite.gd:523`, every proc segment) vs "Rewrite the Rite" (`:521`), "EXIT RITE • LOCKED" (`ui/overlays/GateOverlay.tscn:328`), HUD "SEALED"/"READY" (`ui/screens/HUD.tscn:360`), "GATE UNSEALED • EVAC IN %ds" (`ui/controllers/HudEvacOverlayController.gd:124`), "RITE EXPOSED" (`GateOverlay.tscn:168`), "OUTER RITE DESTABILISED" (`data/narrative/Segment1Text.gd:47`), "follow the orange gate marker" (`core/systems/world/SegmentProcBuilder.gd:602`). Roadmap only ever says Exit Rite.
2. **Armor vs Armour in item/augment text.** "Armour" in `Augment_DoctrineOfBurden.tres:13`, `curse_ashen_ballast.tres:21`, Scar Tissue (`ManifestationCatalog.gd:155`) vs "Armor" in the equipment label (`data/items/Inventory.gd:16`), `Augment_StaminaCore.tres:21`, `conduit_plating.tres:19`, `major_frame_reinforcement.tres:21`, Gravemarch glossary (`Gravemarch.tres:63`). The roadmap itself uses both.
3. **"attempt" vs "run" vs "Ascension" for the same thing.** "NO ACTIVE ATTEMPT" (`ui/components/SaveCard.gd:173`), "(this attempt)" (`major_frame_reinforcement.tres:21` + 3 more) vs "Start Run" (`ui/screens/base.tscn:255`), "SAVES / NEW RUN" (`MainMenu.tscn:203`), "Run Sheet" vs "Death would end the Ascension" (`ui/screens/HubShop.gd:1120`). Docs say "run" 584 times.
4. **"Doctrine" means three things on screen.** "ASCENSION DOCTRINE" screen title (`ui/screens/MajorChoice.tscn:80`) and its middle stage "DOCTRINE" (`MajorChoice.gd:51`), the NEG augment "Doctrine of Burden" (`Augment_DoctrineOfBurden.tres:9`), and a ranged mutation "Scatter Doctrine" (`major_ranged_shotgun.tres:15`). The roadmap knows only Doctrine of Burden.
5. **Segment vs District vs Area.** "AREA 1 · SEGMENT 1" (`SaveCard.gd:173`, `HubShop.gd:384`) vs "District boss defeated" (`SegmentProcBuilder.gd:590`), "District heat" (`ui/controllers/HudThreatController.gd:225`), "THE DISTRICT SHIFTS" (`EncounterDirector.gd:226`), and one doctrine card saying "in every district" and "Once per segment" three lines apart (`apotheosis_manufactured_witness.tres:15,19`).
6. **Wardstones are "Attuned" and "Rewritten" in the same segment.** `Wardstone.gd:133,201` ("Wardstone Attuned", "Stand within the Wardstone to Attune") vs `Segment1Text.gd:15,36`, `Level1Builder.gd:1661,1674` ("Rewrite the first Wardstone", "WARDSTONE REWRITTEN"), `scenes/game.gd:379`.
7. **Stat/slot labels drift across screens.** Health/HP/Max HP/maximum HP; Movement/Move Speed/movement speed; abbreviations MOV vs MOVE and PWR vs POW from two hard-coded tables (`data/items/Inventory.gd:15-22` vs `ui/screens/InventoryStash.gd:14`); roadmap says "Move" and "attack speed" where the game says "Movement" and "Haste".
8. **Followers are also "Support"/"Supporters" in the Exchange.** "CURRENT SUPPORT" (`HubShop.gd:384`, `HubShop.tscn:176`), "Insufficient support" (`HubShop.gd:1111`), "Supporters search the city's…" (`FollowerFeedbackUI.gd:57`) vs "Followers search the city's…" (`HubShop.gd:401`).
9. **Four words for the 16-slot bag, three for equipped gear.** "Backpack" (`HubShop.tscn:236`), "Offer Bag"/"Bag" (`HubShop.tscn:341,424`), "Expanded Satchel … +4 backpack slots" (`major_satchel.tres:14-15`); "Gear"/"GEAR REGISTER" vs "Equipment" vs "Manage Inventory" (which opens the Stash, `HubShop.tscn:196-197`).
10. **Manifestation / rule / engine / protocol; set-ability names.** Tutorial text alternates "Manifestation" and "rule" (`ui/controllers/HudManifestationController.gd:402-409`), tooltip "never reroll its rule" (`ItemTooltip.gd:203`), pair notifier says "Full protocol recorded" (`ManifestationPairNotifier.gd:163`); set names come in three shapes ("Conduit", "Gravemarch Protocol", "Lattice Index") and the Lattice 6-piece is "Triangle Commit" in its title but "Index Commit" in its own description (`data/sets/lattice/Lattice.tres:44` vs `:45,63`).

Honourable mentions: mixed UK/US spelling in adjacent cards ("recognises" `Segment1Text.gd:46` vs "recognizes" `apotheosis_law_of_admission.tres:20`; "authorised" vs "authorized"; "stabilizes" vs "Stabilise"); "sigil" meaning four things; "THE DISTRICT SHIFTS — ASCENSION" colliding with the title; "R%d" vs "r%d"; "Lucky Crit" vs "lucky crits".

Docs that are stale against the code: `docs/design/MANIFESTATIONS.md:157-173` (old family tags instead of the five nouns), roadmap §12.3 "attack speed" (game says Haste), roadmap slot list "Move" (UI says Movement), roadmap omits Overtime / Ascension Doctrine / Exchange / Backpack entirely, and plan docs use "gate" for the Exit Rite (`docs/superpowers/plans/2026-08-27-phase2-one-great-run.md:39,158`, `docs/gamegoal.md:336`).

---

## 3. File / scene / class naming

**class_name ≠ file basename (6 of 489 scripts)**

| File | class_name | Note |
|---|---|---|
| `core/actors/enemy/enemy.gd:2` | `EnemyActor` | snake file, different class |
| `core/systems/enemy_world/EnemyWorld.gd:1` | `EnemyWorldService` | |
| `core/systems/spawner/spawner.gd:2` | `EnemySpawner` | |
| `autoload/ThreatDirector.gd:2` | `ThreatDirectorSingleton` | forced by autoload-name collision; deliberate |
| `ui/screens/AugmentLibrary.gd:2` | `AugmentLibraryScreen` | |
| `ui/screens/settings/BindingRow.gd:2` | `SettingsBindingRow` | |

**File-name casing**: 11 snake_case `.gd` among PascalCase (`enemy.gd`, `player.gd`, `projectile.gd`, `spawner.gd`, `global.gd`, `ui/components/selection_card.gd` vs `SelectionCard.tscn`, `ui/screens/base.gd`, `ui/screens/hud.gd` vs `HUD.tscn`, `scenes/game.gd`, `scripts/boot.gd`, `tools/batch_icon_cutter.gd` vs `BatchIconCutter.tscn`); 8 snake `.tscn`. `.tres`: 51 snake (`data/items`, `data/major_choices`) vs 45 Pascal (`Augment_*`, `EnemySpec_*`, sets, races, styles, weapons). KEEP (rename risk: scene/resource paths), but new files should pick one.

**Scene root node ≠ file name**: all 12 `scenes/world/enemies/Enemy*.tscn` roots are `"Enemy"` (inherited) **except** `EnemySplitter.tscn` (root `"EnemySplitter"`, built via `1_enemy` ext-resource instead of inheritance) and the bosses (`BossArcanist`, `BossBulldozer`). `base.tscn`→"Base", `boot.tscn`→"Boot", `game.tscn`→"Game" (case only).

**Enemy chain (scene → spec → id → display → module)**

| Scene | Spec file | id | display_name | behaviour module (class) | Issue |
|---|---|---|---|---|---|
| `EnemyOrbiter.tscn` | `EnemySpec_Orbiter.tres` | `enemy_orbiter` | Orbiter | `modules/EnemyOrbit.gd` (`EnemyOrbit`) | module named by verb (and dead — see dead-code audit) |
| `EnemySpitter.tscn` | `EnemySpec_Spitter.tres` | `enemy_spitter` | Spitter | `modules/EnemyShooter.gd` (`EnemyShooter`), shared by Herald/Tactical (`EnemyHerald.gd:44`) | "Shooter" appears nowhere in data/UI/docs; 11 code refs |
| `EnemyCharger.tscn` | `EnemySpec_Charger.tres` | `enemy_charger` | Charger | `EnemyCharge.gd` (`EnemyCharge`) | verb vs noun |
| `EnemySplitter.tscn` | **`EnemySpec_SplitterSlime.tres`** | `enemy_splitter` | Splitter | `EnemySplitter.gd` | "Slime" exists only in the spec filename; id is edit-distance 1 from `enemy_spitter` |
| `EnemySummonedMinion.tscn` | `EnemySpec_SummonedMinion.tres` | `enemy_summoned_minion` | Summoned Minion | `EnemySummoned.gd` | missing from `EnemyDossierCatalog` |
| `EnemyBrute.tscn` | `EnemySpec_Brute.tres` | `enemy_brute` | Brute | (none) | "Tank" is never used for it (only `boss_archetype` meta `BossArena.gd:148`, `Augment_StaminaCore.tres:20`) — consistent |
| `BossArcanist.tscn` / `BossBulldozer.tscn` | `EnemySpec_Boss*.tres` | `boss_arcanist`, `boss_bulldozer` (no `enemy_` prefix) | Arcanist / Bulldozer | `BossArcanistBrain.gd` | both missing from dossier (`data/enemies/EnemyDossierCatalog.gd:7-20` covers 14 of 17 specs) |
| (opening) | `EnemySpec_OpeningOfficer.tres` | `enemy_opening_officer` | "Containment Officer" | `OpeningActor` uses `&"officer"` (`OpeningActor.gd:152`) | two ids |
| (opening) | `EnemySpec_ContainmentConstruct.tres` | `enemy_containment_construct` | **"Lattice Construct"** | `OpeningActor` uses `&"construct"` (`:9`) | display collides with the Lattice set name |

Enemy spec ids are persisted (`meta_discovered_enemy_ids`) — do not rename ids; filenames/modules are safe to align.

**Effects / resources**: `data/augments/Augment_ReflectShield.tres` → `effects/augments/scenes/ReflectedShieldEffect.tscn` (root "ReflectedShieldEffect") → `effects/augments/logic/ReflectShieldEffect.gd`; `Augment_HexBlinkMarkEffect.tres` is the only augment whose filename disagrees with its id (`augment_blink_hex`, display "Blink Hex", `:9-10`); `Augment_SummonSpiderlings.tres` ↔ `SpiderlingSummonEffect.gd` (word order); `effects/items/logic/SpeedRingEffect.gd` is the effect attached to `data/items/defs/accessories/ring_crusher.tres:6` (Crusher Ring runs a "SpeedRing" effect); Gravemarch 6P id `gravemarch_6_verdict` in `GravemarchMassArrest.gd:63`; Lattice 6P id `lattice_6_index_commit` in `LatticeEchoBuffer.gd:49` (three names, see #24). Item/set/race/style ids all match their filenames; `data/items/defs/item_test.tres` is a test fixture living in shipping data.

**Tests vs system names**: most match. Named after features/events rather than modules: `AscensionDoctrine{,Gameplay,Presentation}Test` (test `core/systems/major_choice/*` + `ui/screens/MajorChoice.tscn`), `ExchangeIdentityTest` (HubShop), `AuditClosureTest`, `PlaytestRegressionTest`, `PerformanceRootCauseFixTest` (headers confirm they pin dated fixes), `Level1DeterminismProbe` (its own header says "Segment 1"). `WorldTileIntegrationTest` vs `Segment1TileIntegrationTest` vs `ChunkTileRendererTest` have no header comment distinguishing them. `EnemyRepresentationLeaseTest` — "lease" appears once in `core/systems/enemy_world`. 23 tests are `.gd`-only (SceneTree runners) vs the `.tscn` convention. `tools/tests/CursedVaultTest`, `ThreatDirectorPressureTest`, `ExitRiteTest`, `BurdenSystemTest`, `EncounterDirectorTest` match their systems.

---

## 4. StringName literals (`&"..."`)

834 distinct ids, 3,148 occurrences; 360 appear exactly once, 250 of those outside `tools/tests/`. Most singletons are legitimate one-writer ids (signal/event names, flight-recorder counters, group registrations read as `String`). The ones worth attention:

**Near-duplicates that look like drift (verify)**
- `&"dev_grant"` (`ui/widgets/PerformanceOverlay.gd:841`) vs `&"developer_grant"` (`ui/screens/MainMenu.gd:272`) — two follower-transaction reasons for the same action.
- `&"bren_first_follower"` (`core/systems/world/Level1Builder.gd:1464`) vs `&"followers_first"` (`:1095`) — both singletons, both about the first follower.
- `&"chunk_unloaded"` (nav-revision reason, `ChunkManager.gd:1396`) vs `&"chunks_unloaded"` (flight-recorder counter, `:299`).
- `&"retire"` (op, `autoload/EnemyIndex.gd:222,383,452`) vs `&"retired"` (counter, `:443`).
- `&"enemy_spitter"` vs `&"enemy_splitter"` (`EnemySpec_Spitter.tres:9`, `EnemySpec_SplitterSlime.tres:9`) — real but typo-prone.
- `&"wardstone"` (heal source, `player.gd:1128`) / `&"wardstones"` (group, `Wardstone.gd:40`) / `&"wardstone_1"` (milestone, `Level1Builder.gd:70`) / `"wardstone:%d"` (safeguard, `SegmentProcBuilder.gd:342`) / `&"segment1:wardstone:1"` (`Level1Builder.gd:1142`) — five id shapes for one object.
- `&"vault"` (drop source, `CursedVault.gd:103`) vs `&"cursed_vault"` (group, `:34`).
- `&"segment1_synthesis"` (`Level1Builder.gd:1084`) vs `&"synthesis"` (`:68`).
- `&"officer"`/`&"construct"` (`OpeningActor.gd:152,9`) vs `&"enemy_opening_officer"`/`&"enemy_containment_construct"` (spec ids).
- `&"grunt"`, `&"runner"`, `&"brute"` (tests: `EnemyWorldStorageTest.gd:39,73,76`, `EnemyProxyRendererTest.gd:53`) vs `&"enemy_grunt"` etc. — tests use bare kind names the specs never use.
- `&"augment_detonate"` (input action, `InputActionCatalog.gd:26`) vs `&"detonate"` (Manifestation event, `ManifestationState.gd:653`); `&"debug_spawn_filter"` (group) vs `&"spawn_filter"` (reason, `spawner.gd:394`).
- No bare `&"doctrine_of_burden"`, `&"corruption_engine"` or `&"inversion_lens"` exists — the `augment_` prefix is used consistently (all refs listed in `player.gd:570-594`, `BurdenResolver.gd:52`, `BuildIdentity.gd:39-45`, `RunSheetHUD.gd:453-487`).

**Case/underscore-only variants**
- `&"Momentum"`, `&"Shard"`, `&"Ward"` (`tools/tests/BuildIdentityTest.gd:53,88`) vs runtime `&"momentum"` ×42, `&"shard"` ×36, `&"ward"` ×32 — test data does not use real noun ids.
- `&"Master"/"Music"/"SFX"/"UI"` (bus names, `core/settings/SettingsRuntimeApplier.gd:17-20`) vs `&"master"/"music"/"sfx"/"ui"` (settings keys, `SettingsScreen.gd:191`) — deliberate mapping, but easy to confuse.
- `&"ChunkManager"`, `&"FlowFieldNav"`, `&"Level1"`, `&"Spawner"`, `&"WorldDropSpawner"` (node names, `scenes/game.gd:36`) vs groups `&"chunk_manager"` ×29, `&"flow_field_nav"` ×7.
- `&"Button"/"button"`, `&"Label"/"label"`, `&"Panel"/"panel"` (theme type names vs codec/test strings) — benign.

**Dead alias probes (singletons that can never match a resource field)**
- `ui/components/selection_card.gd:80-86` probes `&"hp_bonus"`, `&"arm_add"`, `&"arm"`, `&"spd_add"`, `&"speed"`, `&"move_speed"`, `&"pow_add"`, `&"pow"`, `&"hst_add"`, `&"hst"`, `&"lck_add"`, `&"lck"`; `RaceData`/`StyleData` only define `hp_add/armor_add/speed_add/power_add/haste_add/luck_add` (`data/RaceData.gd:9-15`) — 12 dead ids (the whole script is dead; see the dead-code audit). Same pattern with a String list in `ui/screens/base.gd:220` and cooldown aliases in `ui/widgets/AugmentActiveBadge.gd:368,371` (`active_cd_left`, `cd_left`, `cooldown_left`, `cooldown_time_left`, `time_left`, `active_time_left`, `active_cd_max`, `active_max_cd`, `cd_max`, `cooldown_max`, `cooldown_duration`, `max_cd`).

**Suspicious singletons**
- `&"another action"` (`ui/screens/settings/SettingsScreen.gd:356`) — StringName containing a space; reads like display text.
- `&"style:melee"` (`data/major_choices/doctrines/method_frame_of_ash.tres:33`) — only writer of a colon-tag; confirm the reader parses the `style:` prefix.
- `&"HB_"` (`scenes/world/combat/MeleeSlash.gd:50`), `&"__diagnostic__"` (`EnemyProxyRenderer.gd:7`), `&"exit"` (`PerformanceOverlay.gd:625`), `&"game"` (`AudioManager.gd:73`), `&"hover"`/`&"focus"` (`MajorChoiceCard.gd:89`) vs `&"ui_hover"` (`TactileButton.gd:52`), `&"capture"` (`Wardstone.gd:37`), `&"vendor"` (`HubShop.gd:1427`), `&"trade_undo"` (`HubShop.gd:344`), tile ids `&"dirt"`, `&"mud"`, `&"decal"`, `&"door"`, `&"floor"` (`ChunkManager.gd:614-648`, `Level1Builder.gd:787`) — each has one writer and no `&"…"` reader; readers may use plain Strings, so verify before deleting.

---

## Status 2026-08-30 — nothing executed; two findings overtaken by the 08-29 deletions

No renaming or string unification from this audit has been attempted: every
commit in `c131cd2..b2b1604` is cleanup, save hardening, or an unrelated fix.
All 30 rows of §1 re-verified open at `b2b1604` by fresh grep — spot evidence:
"Armour" `Augment_DoctrineOfBurden.tres:13` vs "Armor" `Inventory.gd:16` (#1);
"recognizes" `apotheosis_law_of_admission.tres:20` vs "recognises"
`Segment1Text.gd:36` (#2); "Scatter Doctrine" `major_ranged_shotgun.tres:15`
(#5); "NO ACTIVE ATTEMPT" (now `SaveCard.gd:189`) vs "Start Run"
`base.tscn:255` (#6); "Full protocol recorded" `ManifestationPairNotifier.gd:163`
(#7); "Hold within the Gate to Escape" `ExitRite.gd:523` two lines under
"Rewrite the Rite" (#10); "Wardstone Attuned" `Wardstone.gd:133` vs
"WARDSTONE REWRITTEN" `Segment1Text.gd:36` (#11); "District boss defeated"
`SegmentProcBuilder.gd:590` / "District heat" `HudThreatController.gd:225`
(#12); "THE DISTRICT SHIFTS — %s" `EncounterDirector.gd:226` (#14);
"CURRENT SUPPORT" `HubShop.gd:384`, "Supporters search"
`FollowerFeedbackUI.gd:57` (#16); "r%d" `BagSlot.gd:199` vs "R%d"
`ItemTooltip.gd:160` (#19); the divergent `EQUIP_HINTS` table
`InventoryStash.gd:14` (#20); "SHARDS" `ManifestationNouns.gd:45` (#21);
"Triangle Commit" vs "Index Commit" `Lattice.tres:44,45,61` (#24);
"Offer Bag"/"Manage Inventory" `HubShop.tscn:341,197` (#25); "this HUB"
`HubShop.gd:219` (#26). §2's ten player-visible items all stand. §3: the six
class_name ≠ basename rows unchanged. §4: the near-duplicates are all present
— `&"dev_grant"`/`&"developer_grant"`, `&"bren_first_follower"`/`&"followers_first"`,
`&"retire"`/`&"retired"` still at `EnemyIndex.gd:222,383,452`/`:443` even
after `7bfc6cb`'s removals, `&"another action"` `SettingsScreen.gd:356`; and
`BuildIdentityTest.gd:53,88` still uses `&"Momentum"/&"Shard"` — `13c7d0f`
edited that block but changed only the luck fixture.
`docs/design/MANIFESTATIONS.md:157-173`'s stale family column is unchanged
(`2f40501` fixed citations in other docs only).

**Overtaken by deletion (2 findings)**

- §3 enemy chain, Orbiter row: `modules/EnemyOrbit.gd` deleted in `0428e8d`;
  zero references remain. The Orbiter has no behaviour module now, so the
  "module named by verb" issue is moot; scene/spec/id/display still align.
- §4 dead alias probes, first bullet: `ui/components/selection_card.gd`
  deleted in `ae86c60`, taking the 12 dead alias ids with it. The two
  companion probe sites stay open: `ui/screens/base.gd:216-220` (String
  alias lists) and `ui/widgets/AugmentActiveBadge.gd:367-370` (cooldown
  aliases).

**Counts and citations gone stale (findings themselves unchanged)**

- §3 casing: `selection_card.gd` (`ae86c60`) and `scripts/boot.gd` +
  `scenes/boot.tscn` + `scenes/_dev/sprite_2d.tscn` (`7a432cb`) are gone —
  now 9 snake `.gd` (was 11) and 6 snake `.tscn` (was 8); strike
  `boot.tscn`→"Boot" from the root-node list. Script total 489 → 481
  (11 deleted across `7a432cb`/`ae86c60`/`0428e8d`/`af4e23e`, 3 new test
  scripts); the six mismatch rows are unaffected.
- §3 tests: `FlowFieldAllocationBenchmark` removed in `af4e23e` — 22
  `.gd`-only tests, was 23.
- §1 #12: `SegmentPlan` deleted in `0428e8d` — strike it from the evidence
  list; the row itself is open as quoted above.
- Line drift from later fixes: `SaveData.gd` is 140 lines after `054f635`
  (preamble cite `:4-121` → `:4-140`; `attempt_pending_big_choice` :66→85,
  `attempt_big_choice_source_segment` :70→89, `attempt_major_choice_id`
  :73→92, `attempt_doctrine_*` :81-87→100-106, `total_runs` :18→37) — every
  persisted field family the risk ratings rest on is unchanged, and
  `054f635` now names the save-critical ones frozen in the header.
  `SaveCard.gd` after `9453f95`: :173→189, :205→221.
  `ManifestationCatalog.gd` after `b2b1604` (+1/+9 lines): SLOT consts
  :16-23→17-24, "Travelling" :59→68, Scar Tissue "Armour" :155→164,
  `BOND_*` :280-316→289-325.

None of the other 08-29 deletions touches this audit: `CoverWindow.gd`,
`VisionOverlay`, `HudInventoryController` and `InventorySlot.tscn` are never
cited here (`InventorySlotView.gd`, cited in #7, is a different, live file).

**Correction to this audit**: #2 lists "cancelled" `SettingsScreen.gd:163`
among UK spellings in player text — that occurrence is a signal name
(`InputCaptureOverlay.gd:5` declares `signal cancelled`), never displayed;
it belongs with the code identifiers the audit says to KEEP. The
"UNIFY-UI (5 US strings)" action is unaffected.
