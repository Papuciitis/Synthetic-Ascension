# Performance war room, 2026-09-19

What the enemy simulation costs today at 60 / 100 / 180 / 300 live
enemies, what the last month of captures says about real-play lag, what is
already built, and a prioritized plan with a benchmark gate per milestone.
Measurements here are headless CPU truth on this machine (i7-8650U, Godot
4.7.2); rendering is measured separately and only on a display.

Sources: `tools/tests/EnemyHordeBenchmark.tscn` (fresh runs, results in
`performance_results/benchmarks/`), `tools/tests/ChunkColdStreamProbe.tscn`
(new), `tools/tests/ChunkStreamingPerformanceAudit.tscn`,
`docs/diagnostics/2026-09-06-sustained-combat-profile.md`,
`docs/audits/2026-09-15-performance-captures.md`,
`docs/PERFORMANCE_PATCH_CHANGELOG.md`, `docs/OPTIMIZATION_HANDOFF.md`, the
build simulator's frame columns, and the code under
`core/systems/enemy_world/`, `autoload/EnemySimulationScheduler.gd`,
`core/combat/projectile/`, `core/systems/world/ChunkManager.gd`.

## 1. What is already built (do not redesign this)

| Concern | State | Where |
|---|---|---|
| Node-based versus data-oriented enemies | Logical population lives in `EnemyWorld` data records; only the nearest `materialized_budget` 64 (hard ceiling 96) are `CharacterBody2D` actors, activated within 480 px and released past 640 px, at most 4 promotions and demotions per step. Everything else is a data-only proxy. | `EnemyRepresentationPolicy` |
| Proxy simulation | 10 Hz, sliced (6 slices normal, 8 under pressure, 12 in emergency), steered by the threaded flow field; hit through data-side radius and segment queries; proxy deaths pay Followers and Luck like actors and emit `enemy_defeated`. | `EnemyProxySimulation`, `EnemyCombatService` |
| Proxy rendering | MultiMesh renderer for proxies; MultiMesh blockers per chunk; impact bursts batched. | `EnemyProxyRenderer`, `ChunkBlockRenderer`, `ImpactBurstRenderer` |
| Update-frequency tiers and AI LOD | Actors sit in full / mid / far tiers (budgets 32 / 32, mid in 3 groups, far in 7), spatial bands (full to 1200 px, mid to 1800 px, hysteresis), adaptive budgets under physics pressure (8 ms) and budget pressure (14 ms), an emergency tier at 20 ms (8 / 16) and a severe fast path at 40 ms, smart-physics release distances per pressure state. | `EnemySimulationScheduler` |
| Spatial partitioning | `EnemyIndex` spatial hash (64 px cells) with population caps by special kind (summoned 36, split 48, boss adds 24, beats 24, specials 72). | `autoload/EnemyIndex.gd` |
| Stale cleanup | Spawner culls at 90% of cap (8 per tick, 0.75 s), pools ambient scenes (32 per scene, warm 6), elites protected from culling and demotion. | `spawner.gd`, `EnemySimulationScheduler._is_protected` |
| Projectiles | Ordinary ballistic projectiles run in dense arrays (capacity 4096) in `_process` (moved off the physics step in August); exotic homing / beam / mine attacks stay node-owned by design. Barrage fragments retarget through a data-side lowest-health scan, at most 48 reacquisitions per frame (September 16). | `ProjectileSimulationManager`, `EnemyWorld.lowest_health_in_radius` |
| Tree attack budget | Slash and impact attacks generated by the Ascension tree are queued and flushed 12 per frame (September 15 fix of the chain-burst lag). | `AscensionRunner.flush_attacks` |
| Chunk streaming | Desired chunks planned per re-centre, activated from a queue under a 2 ms budget and a per-frame cap; blockers batched; build phases timed per chunk (setup, ground, content, floor, blocker), exposed to the overlay and, since today, the newest chunk's phase split to every flight-recorder sample. | `ChunkManager` |
| Instrumentation | Flight recorder samples carry wall-clock spacing, the tree's per-frame costs, projectile time and chunk stream stats; incidents at 28 ms frames; `tools/perf/analyze_captures.py`; deterministic headless benchmarks for hordes, pressure, projectiles, Barrage chains, chain bursts, the recorder itself. | `PerformanceFlightRecorder`, `tools/tests/*Benchmark*` |

## 2. Measurements

### 2.1 Headless horde at the requested populations (today's code)

`EnemyHordeBenchmark`, one stage per run, 20 s hold after a 4 s spin-up,
segment 2 world, player stationary, ranged style, god mode:

| Live enemies | Frame avg / p95 / p99 (ms) | Process p95 | Materialized (actors) | Data-only | Tier full / mid | Physics on |
|---:|---|---:|---:|---:|---|---:|
| 60 | 6.97 / 7.14 / 8.33 | 7.56 | 60 | 0 | 32 / 28 | 60 |
| 100 | 6.96 / 6.94 / 6.94 | 11.75 | 64 | 36 | 32 / 32 | 64 |
| 180 | 6.94 / 7.14 / 8.33 | 10.17 | 64 | 116 | 32 / 32 | 64 |
| 300 | 7.04 / 7.22 / 9.98 | 12.74 | 64 | 236 | 32 / 32 | 64 |

The August 27 runs of the same benchmark: 120 -> 8.3 ms p95, 250 -> 16.5,
400 -> 14.1, 550 -> 27.2 (physics p95 38). Reading: past 64 live actors
the cost curve is flat because the population above the budget is
data-only; the 180 barrier the project started from no longer exists in
the simulation. The `physics p95` column is Godot's windowed monitor and
includes render sync; it is not a per-frame physics cost (recorder note,
September 16).

### 2.2 What real play measured (captures, rendered)

- September 6 profile: frame p95 rises steeply after about 80
  *materialized* enemies; process side dominant in 97 of 106 incidents;
  projectile and full/mid-tier work scale with the same population; flow
  rebuilds and the recorder's own snapshots are not the sustained cause.
- September 14: the fight lag was the tree's kill chains spawning slash and
  impact nodes per kill (physics 337 ms, 12k draw calls); fixed by the
  attack budget (September 15).
- September 15: Barrage fragment reacquisition (51 -> 3.3 ms per update
  at 120 forced retargets after the fix), recorder deltas that were not
  wall-clock (fixed), and chunk activations of 30-77 ms mid-run with 1-7
  cached enemies (open).
- Build simulator (this week, headless): the worst per-build p99 frames
  are set VFX churn (Lattice's Index Commit spawns up to 19 VFX nodes per
  triangle, Mass Arrest's magic path ten impacts per slam), 16-30 ms at
  p99 and up to 75 ms max; Lattice builds run a 4.5 ms p95 median against
  1.4 ms for the other sets.

### 2.3 Chunk streaming, cold (new probe, today)

`ChunkColdStreamProbe`: a fresh seed, the centre walking one chunk east
per step, every queued chunk activated each step, per-phase costs
collected.

| Seed / steps | Activations | Total p50 / p95 / max (ms) | Content share | Over 2 ms | Over 16.7 ms | Worst chunks |
|---|---:|---|---:|---:|---:|---|
| 902611 / 10 | 75 | 0.40 / 2.69 / 43.96 | 73% | 9 | 2 | (0,-2) initial 43.96 (content 42.6); (-2,-1) initial 23.4 (content 22.3) |
| 123457 / 20 | 125 | 0.41 / 1.98 / 40.14 | 65% | 6 | 2 | (0,-1) initial 40.1 (content 39.1); (5,0) streamed 23.1 (content 22.1) |
| 123457 / 40 | 225 | 0.38 / 1.99 / 40.46 | 59% | 11 | 2 | the same two chunks; nothing else above 2.5 ms |
| 555001 / 40 | 225 | 0.38 / 2.03 / 41.95 | 60% | 14 | 2 | (0,-1) initial 41.95; (-1,2) initial 22.8; nothing else above 3.2 ms |

Three facts. Setup, ground, floor and blocker phases never exceed 3 ms.
Exactly two activations per run exceed 16.7 ms whatever the distance
walked, and both are made of the same things as the cheap chunks
(28-33 children, 2-3 static bodies, 25-30 sprites): they are one-time
cold costs of a session (the first chunk ever generated, about 40 ms,
and the first chunk of a second kind, about 22 ms: resource and scene
loads), not a per-chunk phase. The warmed audit pre-builds the first chunk
and then holds median < 4 ms and max < 12 ms on today's code. And the
generic manager the probe drives never decorates a site (0 site chunks in
every run), so the September 15 mid-run activations of 30-77 ms with 1-7
cached enemies are **not reproduced headless here**: they happen on the
real segment world (districts, parcels, sites, cached-enemy
re-materialization on activation) and need the reproduction in M1 before
anything is staged. The site decoration now carries sub-step timers
(plan, floor, walls, cover, door, volumes) in the stream stats for that
run.

### 2.4 Chunk streaming on the real segment-2 world (new probe, today)

`RoamStreamProbe`: the real game like the horde benchmark, a 120-enemy
horde held by the debug spawn filter, the player teleported one chunk
(2,048 px) east every 2 s for 16 stops, the game `ChunkManager` sampled
every frame. Two runs, two seeds (`start_new_attempt` rolls one):

| Run | Activations while roaming | Total p50 / p95 / max (ms) | Content p50 / p95 / max | Blocker p50 / p95 / max | Over 16.7 ms | Roaming frame p50 / p95 / p99 / max |
|---|---:|---|---|---|---:|---|
| 1 | 48 | 4.67 / 12.92 / 19.70 | 2.04 / 6.09 / 7.43 | 2.44 / 6.62 / 11.83 | 1 | 6.90 / 6.94 / 7.41 / 30.51 |
| 2 | 48 | 6.16 / 12.18 / 99.63 | 2.73 / 5.01 / 98.63 | 2.81 / 6.75 / 7.23 | 1 | 6.90 / 7.14 / 8.33 / 61.57 |

The blocker phase splits into physics (bodies and shapes, 3.6-6.9 ms on
the worst chunks) and the MultiMesh renderer's add (2.2-4.9 ms). The site
decoration itself is cheap (2-4 site chunks per run, 2.6 ms on the one
printed). So on the real world an ordinary parcel chunk costs 12-20 ms
to activate (content 5-7 + physics 4-7 + render 2-5), the queue's 2 ms
budget admits one such activation per frame, and a 7 ms frame becomes a
12-20 ms one: the steady mid-run hitch. These are headless CPU numbers;
rendering adds to them.

**Named, with the probe on the plan's main route (seeds 11 / 22 / 33,
content and district sub-steps timed).** Walking east through unplanned
grass never reproduced the hitch (max 11 ms, all site chunks); following
`main_route` through the district did, and the content generator's
sub-step timers name every heavy phase:

| Phase | Chunk kind | Cost (ms) | Cause |
|---|---|---|---|
| content, "roads" step | the primary plaza, the wardstone court, an optional interior: the first chunk to stamp road / plaza texture 7, 3 or 8 | 24-31 per texture, 99 when three coincide | `WorldArt.ground_texture` loads each ~1 MB ground PNG on first use; the warm-up only covered the base and terrain indices, so the first plaza paid the stamps' textures inside its activation. |
| content, "fill" step | `district_fill` (urban envelope courtyard blocks) | 4-8 (15 once) | `decorate_urban_fill`: parcel carving and building instantiation, real work per chunk. |
| blocker physics | every parcel chunk | 4-7.5 | `ChunkBlockPhysics.build`: one shape per merged wall interval, 40-120 per chunk. |
| blocker render | every parcel chunk | 2.5-5 | `ChunkBlockRenderer.add_chunk`: one MultiMesh instance per blocker cell. |
| site decoration | 1-2 per route | 3-5 | plan 1-2 + walls 1.5-2.5; not a hitch. |

**M1 result (built today, headless, three seeds).** M1a warms every
ground texture at world configure time (`ChunkManager._warm_ground_textures_for_plan`);
M1b stages a streamed chunk's blocker work over the queue steps after its
content activation: bodies on the next step, the MultiMesh add on the one
after, blocked cells still registered at content time so navigation is
unchanged (`staged_blocker_activation`, on by default; explicit-limit
queue calls stay synchronous and flush first).

| Seed | Before: worst activation (ms) | After M1a: worst activation | After M1a+M1b: worst single-frame step (p50 / p95 / max) | Steps over 12 ms | Roaming frame p95 / p99 / max |
|---|---:|---:|---|---:|---|
| 11 | 33.7 (wardstone court, textures) | 13.6 | 3.05 / 5.99 / 7.07 | 0 | 6.94 / 7.41 / 20.2 |
| 33 | 33.1 (primary plaza, textures) | 16.5 | 3.75 / 6.31 / 8.44 | 0 | 7.14 / 8.33 / 19.2 |
| 22 | 29.6 (primary plaza, textures) | not rerun | not rerun | | |

The streaming step is now bounded at about 8 ms on these seeds: the
content side of a `district_fill` chunk (5-8 ms) or its physics (4-7.5
ms), each on its own frame. The roaming frame's max (19-20 ms) is no
longer a streaming step; the recorder's hitch tagging (M3) is what names
it next. Gate for M1c (split the parcel content itself) is not met by
need: no step above 12 ms on three seeds, so M1c stays unbuilt.

### 2.5 The simulator's melee tail, named (after M2)

The build simulator's frame time is the cost of its own scripted frame
(the recorder's window, the native strike and every hook it fires, Q / V
/ dash, scripted pressure, the crowd refill); the engine's own frame runs
between samples. Each step is now timed and the slowest 1% of frames
vote for the step behind them (`step_p95_ms`, `tail_step` per build).
Melee-only runs, one build per Core per tier plus the presets, seed 77,
480 frames, 60 bodies:

| Set pinned | Melee builds | Frame p95 / p99 (ms) | Strike step p95 / max | Tail step |
|---|---:|---|---|---|
| Lattice | 27 | 6.75 / 11.92 | 6.70 / 22.6 | strike, 27 of 27 |
| Conduit | 27 | 3.24 / 5.58 | 3.10 / 12.3 | strike |
| Gravemarch | 27 | 2.02 / 3.97 | 1.79 / 9.5 | strike |

The strike step is the synchronous hit chain of one native melee hit:
under Lattice, a triangle fires four area pops over the 60-body crowd,
which is up to 240 `apply_damage` calls in one frame. The combat
service's per-hit stages, timed under a debug flag
(`EnemyCombatService.debug_timing`), split a Lattice melee strike
(p95 6.35 ms) as:

| Piece | p95 (ms) | Share | What it is |
|---|---:|---:|---|
| `enemy_damaged` listeners | 1.93 | 30% | the balance recorder's ledger: provenance from the hit's tags, metrics, per-enemy rows (7 us per hit in isolation, more with a multi-contribution HitLedger) |
| other | 2.14 | 34% | gather in radius, knockback and stun per handle, `_adjust_damage`, `damage_dealt` listeners, the runner's wrapper, VFX |
| tree hit handling | 0.86 | 14% | `AscensionRunner` hit resolution and the engines' `on_hit` |
| death path | 0.83 | 13% | defeat signal, proxy death payout, transaction, removal |
| `player_hit_landed` listeners | 0.40 | 6% | Manifestation hooks |
| BattleText | 0.34 | 5% | one floating number per hit |

So the melee tail is per-hit fan-out at roughly 25 us a hit, not VFX
churn (M2) and not the tree. It is the same fan-out a Barrage fragment
storm or a 300-body horde pays per hit in play, which makes the per-hit
overhead a target of its own (M8 below).

## 3. What the numbers do and do not say

- The simulation side is solved for the counts you asked about: 300 live
  enemies cost the same frame as 60 because 236 of them are data. The
  remaining CPU risks are the *materialized* 64 under combat load (2.2),
  set VFX churn, and site-chunk activation.
- Rendering is unmeasured in every number above. The August desktop runs
  put render CPU at about 1.5 ms and GPU about 5 ms at 550; nothing since
  has re-measured it after the V4 tree, the sets' VFX and the proxy
  renderer changes. Every "playable" claim needs a rendered capture.
- Real play has three other lag sources the benchmarks do not exercise:
  the player moves (streaming, flow field re-centre), builds fire dozens of
  payload kinds per frame, and enemies die in bursts (drops, VFX, kill
  chains). The captures, not the benchmarks, are the judge.

## 4. Prioritized plan

Ordered by expected frame-time return per unit of risk. Each milestone
names its gate; a milestone ships only with its gate green and the
previous milestones unchanged.

| # | Milestone | Design | Gate (benchmark) | Risk |
|---|---|---|---|---|
| M0 | **Rendered baseline** (needs the display) | Run `EnemyHordeBenchmark` windowed at 60 / 180 / 300 and a stationary Endless Lunge + Mass Grave capture at seg6 with the recorder's extended features; record render CPU / GPU from `viewport_get_measured_render_time_*`. | A filed table in this document; no code. | none |
| M1 | **Stage the real-world activation** (DONE, section 2.4) | M1a (built): warm every ground texture at `configure_procedural_world`, since the district generator stamps roads, sidewalks, plazas, aprons and mud from the same list and the first plaza paid three first-touch PNG loads inside its activation. M1b (built): stage a streamed chunk's blocker physics and MultiMesh add over the two queue steps after its content activation; blocked cells are registered at content time, so navigation and the walkability snapshot are unchanged; an unloaded chunk's pending steps are dropped. M1c (not needed): split parcel content generation into plan and instantiate, only if a step exceeds 12 ms on a route. | `RoamStreamProbe` on the plan's main route: no step above 12 ms on seeds 11 and 33 (max 7.1 / 8.4 ms, was 33.7 / 33.1 per activation); `ChunkStagedBlockerTest` (27 checks: stages follow content, staged equals synchronous, unload before a stage, explicit-limit calls synchronous, textures warm), `ChunkStreamingPerformanceAudit`, `ChunkStreamingSchedulerTest`, `ChunkStreamReplanThrottleTest`, `ChunkBlockIntegrationTest` green. | low: a chunk's blockers invisible for one queue step while already colliding, its bodies absent for one step (the chunk is in the prefetch ring, not under the player). Not verified rendered. |
| M2 | **Set VFX pooling** (BUILT; the tail it was meant for did not move) | `PooledVfx` routes the five conduit VFX (pulse ring, spokes, arc line, cleave arc, shockwave) through the `PoolManager`: reset on reuse (`_on_pool_obtain`), one shared additive material instead of one per burst, 48 retained per kind, a live cap of 64 per kind that refuses further spawns (callers already null-check), `queue_free` fallback for a VFX the pool never saw. Lattice, Gravemarch and Conduit spawners obtain through it. | `SetVfxPoolTest` (21 checks: reuse and reset per kind, live cap and retention, arc-line fade, unpooled fallback, churn: 30 rings per frame for 150 frames, raw p50 20.1 ms vs pooled 6.9 ms headless). Build simulator, Lattice pinned, 62 builds, seed 77, before vs after: melee p95 6.47 vs 6.45, p99 11.70 vs 11.64, ranged and magic within noise. **So the P4 reading (set VFX churn as the melee p99 tail) is not confirmed headless**: Bastion builds, which spawn few set VFX, top the p99 list at 12-15 ms both times. The pool stays (it is a real per-burst saving and a cap on burst storms); the melee tail is a separate question, next. | low: a reused burst starts from its first frame with fresh randomness; visuals unchanged by design. Not verified rendered. |
| M3 | **Hitch tagging** (DONE) | `PerformanceHitchTagger` (static, thread-safe): every sample over 28 ms wall time is charged to the largest measured cost it carries: `ascension` (tick + flush + hit handling, fragments taken out), `fragments` (Barrage), `projectiles`, `chunk_<phase>` (a new activation, or a staged blocker step when the same chunk's sample grew), `flow` (snapshot + publish on a revision change), `lifecycle` (attach / detach / retire deltas), `enemy_step` (the scheduler's physics step), `sampling`, else `physics_monitor` when Godot's monitor covers half the frame, else `unattributed`; a cost must reach 2 ms and a quarter of the excess over 16.7 ms to claim the frame. The summary carries `hitch_count`, `hitch_tags` (by count, with worst frame and cost) and `worst_hitches`; the incident CSV gains `wall_ms, ascension_usec, fragment_usec, projectile_ms, chunk_build_ms, flow_publish_usec, hitch_tag, hitch_ms`; `analyze_captures.py` prints the tag distribution when the column exists. | `PerformanceHitchTaggerTest` (23 checks: each tag from synthetic samples, staged chunk steps, the summary distribution and the CSV columns); `FlightRecorderSampleTest`, `PerformanceFlightRecorderTest` green. The next playtest's incidents carry tags; nothing rendered yet. | none: read-only over fields already sampled. |
| M4 | **Materialized budget decision** (yours) | Either raise `materialized_budget` 64 -> 96 (the ceiling) so proxies near the player deal contact damage sooner, or give proxies within 200 px a data-side contact tick. Both are gameplay decisions; the August handoff left them open. | Horde 180 / 300 p95 within 2 ms of today at 96; playtest feel. | design |
| M5 | **Scheduler refresh cost** | Profile `EnemySimulationScheduler` assignment (0.2 s interval, incumbent bias 0.9) at 300-550 live; move the ranking to a data-side pass over `EnemyWorld` arrays if it shows above 1 ms per refresh. | Horde 550 p95 from 27 ms toward 20 ms; `EnemyPressureBenchmark` gate (20% over its baseline arm) kept. | medium |
| M6 | **Rapier 2D A/B** (your go-ahead) | The biggest remaining physics lever for the 64 actors (August: 30 ms p95 at about 90 bodies); third-party binary, may change `move_and_slide` feel. | Horde 300 physics monitor and the pressure benchmark's physics step, both arms. | high: feel |
| M7 | **GDExtension hot loops** | Only if M5 and M6 leave the proxy slice or the projectile `_simulate_one` above budget at 550+: proxy step, batch fill, projectile sim. | Minigun benchmark massacre stage p95 from 39 ms toward 25 ms. | high: build pipeline |
| M8 | **Per-hit overhead** (from section 2.5) | Cheapen the fixed cost every `apply_damage` pays: memoize `BalanceAttribution.from_tags` by tag signature (a hit's tags repeat thousands of times per run), coalesce BattleText to one number per handle per frame, batch knockback and stun into the world arrays instead of per-handle calls, and let the recorder's `enemy_damage` skip the per-enemy row work for handles it has already seen this window. | Build simulator, Lattice pinned, melee only, seed 77: strike step p95 from 6.35 ms to under 4 ms with identical kills and HP removed; `BalanceRecorderLoadTest` ledger cost from 7.2 us per hit toward 4; `BalanceAttributionTest`, `BalanceLedgerTest` green. | low-medium: attribution and ledger totals must not change (the tests compare them); BattleText coalescing changes what the player sees on multi-hit frames. |

Not on the list because they are done: attack budget (September 15),
fragment targeting and recorder trust (September 16), projectile
simulation in `_process`, impact VFX batching, MultiMesh proxies and
blockers, population caps by special kind (the summoner snowball of the
August handoff is closed by `summoned_population_cap` 36).

## 5. Verification protocol for every milestone

1. Headless: the milestone's gate benchmark, three runs, medians reported.
2. Regression: the suites named in the gate, plus `ScriptParseAuditTest`.
3. Rendered (display): a two-minute seg6 capture with the recorder's
   extended features on the same route and seed as M0, incidents tagged
   (M3), compared with the M0 table.
4. Filed under `performance_results/benchmarks/` and one dated paragraph
   in `docs/PERFORMANCE_PATCH_CHANGELOG.md`.

## 6. What was changed today

- `ChunkColdStreamProbe.tscn`: the cold, walking, per-phase streaming
  profile (section 2.3) with each heavy chunk's composition, read-only.
- The flight recorder's `chunk_stream` block carries the newest chunk's
  phase split, so an incident during an activation names the phase;
  `SiteOverlayImpl.decorate_chunk` times its six sub-steps into the
  stream stats (`site_last_step_usec`). `FlightRecorderSampleTest` and
  `PerformanceFlightRecorderTest` pass.
- Fresh horde numbers at 60 / 100 / 180 / 300 (section 2.1).
- `RoamStreamProbe.tscn`: the real-world roaming reproduction (section
  2.4); follows the plan's main route (`ROAM_ROUTE=east` for the old
  walk), takes `ROAM_SEED`, prints each heavy activation's content and
  district sub-steps and the worst single-frame step per activation. The
  blocker phase's physics / renderer split is in every phase sample;
  `ChunkGenImpl.debug_last_content_steps` and
  `ChunkGenDistrict.debug_last_step_usec` carry the content split into
  the stream stats (`content_last_steps`).
- M1a and M1b built (section 2.4 and the plan table): every ground
  texture warmed at configure time; blocker physics and renderer staged
  over the queue steps after content, `staged_blocker_activation` on the
  `ChunkManager`; `ChunkStagedBlockerTest` added; stream stats expose
  `pending_blocker_stages`, `last_blocker_stage_ms`, `max_blocker_stage_ms`.

- M3 built: `PerformanceHitchTagger` names the subsystem behind every
  sample over 28 ms; the summary, the incident CSV and
  `analyze_captures.py` carry the tags; `PerformanceHitchTaggerTest`.
- M2 built: `PooledVfx` and the five conduit VFX through the
  `PoolManager`; `SetVfxPoolTest`. The simulator's Lattice tail did not
  move, which retires the P4 reading (plan table).

- The build simulator times its scripted steps (`step_p95_ms`,
  `tail_step`, the strike split) and takes `SIM_CORES`; the combat service
  carries a debug-only per-hit timer. Section 2.5 names the melee tail.

M4-M8 are not implemented. M0 (rendered baseline) still needs the
display; the first rendered capture after this commit is the check that
the tags name real causes (M3) and that the staged activation holds up
with draw cost on top (M1).
