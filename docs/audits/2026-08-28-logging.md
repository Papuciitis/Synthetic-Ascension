# Logging and error-message audit

**Date:** 2026-08-28 · **Tree:** `enemy-world-work` @ `c131cd2` · **Kind:** read-only (nothing modified).
**Scope:** `autoload/`, `core/`, `scenes/`, `ui/`, `effects/` (`tools/tests` excluded). Debug *flags* are covered by the cleanup audit; this covers the *messages*.

## 0. Inventory

**191 call sites in 51 files**: `print` 77, `push_warning` 79, `push_error` 34, `prints` 1; `print_rich` / `printerr` / `print_debug` / `assert` **0**. (One `print` is commented out: `EnemyDrops.gd:70`.)

| File | Count | File | Count |
|---|---|---|---|
| autoload/global.gd | 20 | core/systems/inventory/WorldDropSpawner.gd | 3 |
| core/actors/player/player.gd | 19 | core/systems/manifestations/ManifestationRunner.gd | 3 |
| autoload/SaveManager.gd | 9 | autoload/SfxManager.gd | 3 |
| core/actors/enemy/modules/EnemyDrops.gd | 9 (8 live) | core/actors/enemy/enemy.gd | 2 |
| scenes/game.gd | 9 | core/systems/world/proc/DistrictPlan.gd | 2 |
| effects/augments/logic/StaminaCoreEffect.gd | 9 | core/systems/world/proc/ChunkGenImpl.gd | 2 |
| ui/screens/base.gd | 7 | core/systems/major_choice/effects/MCE_GrantItemRoll.gd | 2 |
| ui/screens/hud.gd | 7 | core/systems/items/ItemEffectRunner.gd | 2 |
| core/systems/spawner/spawner.gd | 6 | core/systems/augments/AugmentRunner.gd | 2 |
| ui/augments/AugmentSelect.gd | 6 | scenes/world/events/BossArena.gd | 2 |
| autoload/DevSetCollisionTools.gd | 5 | scenes/world/events/MiniBossArena.gd | 2 |
| core/settings/SettingsStore.gd | 5 | ui/screens/SaveSelect.gd | 2 |
| core/systems/world/SegmentProcBuilder.gd | 5 | ui/components/BagUI.gd | 2 |
| effects/augments/logic/HexBlinkMarkEffect.gd | 5 | ui/controllers/AugmentTooltipController.gd | 2 |
| effects/augments/logic/MagicMissileEffect.gd | 5 | ui/controllers/HudBagController.gd | 2 |
| effects/augments/logic/ReflectShieldEffect.gd | 4 | ui/controllers/HudTooltipController.gd | 2 |
| scenes/world/pickups/ItemPickup.gd | 4 | effects/augments/logic/SpiderlingSummonEffect.gd | 2 |
| ui/controllers/HudInventoryController.gd | 4 | 16 files with 1 each | 16 |

**Guard status of the 78 `print`/`prints`:** 15 behind `const DEBUG_GLOBAL := false` (dead in every build); 25 behind an `@export`/instance flag defaulting `false` (scene overrides confirm `debug_spawns = false`, `debug_drops = false`); 16 behind `OS.is_debug_build()` (ship in debug exports); 1 dev-console only; **5 behind a flag that defaults `true`** (`MagicMissileEffect.gd:12 @export var debug_prints: bool = true`, not overridden by its scene); **16 with no guard at all** (StaminaCoreEffect ×9, game.gd:118/297/406, base.gd:96/149/176, SegmentProcBuilder:114).

`push_warning`/`push_error` print in release exports too, so every unguarded warning path below is player-visible in the log.

---

## 1. Findings by category

Format: **severity / confidence** — `path:line` — current → problem → replacement → gating.

### (a) Flood risk: per-frame / per-spawn / per-hit / per-press

1. **HIGH / high** — `effects/augments/logic/MagicMissileEffect.gd:74` — `"[MM] FIRE burst at handle:"` fires every burst (0.75 s ÷ haste → 1–3 lines/s for the whole run) whenever an enemy is within 750 px; `debug_prints` defaults `true` → **ships in release**. → Flip the default to `false`; `[MagicMissile] fire handle=%d dmg=%.1f cd=%.2f`.
2. **HIGH / high** — `MagicMissileEffect.gd:62` — `"[MM] no enemies in radius=… (group must be 'enemies')"` 1 line/s while idle, ships; the hint is **wrong**: `_find_nearest_enemy` (line 132) is `EnemyWorld` handle-based. → `[MagicMissile] idle: no valid EnemyWorld handle within radius=%.0f`, same flag, keep the throttle.
3. **HIGH / high** — `MagicMissileEffect.gd:30,37,42` — :30 prints a `<PackedScene#…>` object id; :42 is a real content error throttled as a `print` instead of a one-shot `push_error`. → :30 `[MagicMissile] ready scene=%s slot=%d` (resource_path); :42 `push_error(... unassigned on %s)` once, then `set_process(false)`.
4. **HIGH / high** — `effects/augments/logic/StaminaCoreEffect.gd:37,57,64,84,92,95,100,105,116` — the only augment effect with **no `debug_prints` flag**; every key press produces 1–3 lines in release; `player is NULL`, `vfx_aura_scene is NULL`, `player has no Hurtbox`, `aura instantiate failed` are genuine failures hidden as `print`. → Add `@export var debug_prints := false` (mirror HexBlinkMarkEffect); make the NULL cases one-shot `push_warning` with `scene_file_path`; drop :116.
5. **MED / high** — `ui/controllers/HudEvacOverlayController.gd:59` and `ui/controllers/HudThreatController.gd:59` — identical "ThreatDirector not found" twins, both from `_process` while `_td == null` → **120 warnings/s** in any scene without the autoload. → Warn once, from one place.
6. **MED / high** — `core/systems/spawner/spawner.gd:342` — "No enemy scene assigned" inside `_spawn_one` → continuous stream if misconfigured, and the run then has no enemies. → `push_error` once with `spawn_table` path; `spawning_enabled = false`.
7. **MED / high** — `core/systems/world/proc/ChunkGenImpl.gd:215` — cover scenes unassigned, emitted per generated chunk. → one-shot `push_error`, stop generating.
8. **MED / high** — `core/actors/enemy/modules/EnemyDrops.gd:136,139,182,187` — per-kill drop failures with no enemy context (latent: all 12 specs set `item_pickup_scene` today). → `[EnemyDrops] %s: no drop — pool empty for prefixes=%s fallback=%s` with `spec.id`; once per pool-cache signature.
9. **MED / medium** — `core/actors/enemy/enemy.gd:210,775` — `push_error` per pool obtain with no handle/enemy. → add `handle`, `spec.id`, `pool_warming`; rate-limit.
10. **LOW / high** — `core/systems/world/proc/DistrictPlan.gd:773` — one line per sealed chunk in a loop. → aggregate: `[DistrictPlan] seed=%d dropped %d sealed envelope chunks: %s`.
11. **LOW / high** — `scenes/world/events/BossArena.gd:189,198`, `MiniBossArena.gd:181,190` — re-emitted every second while the trigger re-fires. → once, with arena name.
12. **LOW / high** — `player.gd:483`, `ItemPickup.gd:156,159`, `ManifestationState.gd:299` — fine in frequency; add ids (see (b)).

Gated-but-correct per-event prints (no action): `spawner.gd:459,462,979,1146`, `ConduitArcBolts.gd:37`, `HexBlinkMarkEffect.gd:79,87,116`, `ReflectShieldEffect.gd:129,295`, `EnemyDrops.gd:64,170`, `ItemPickup.gd:118`.

### (b) Messages without context

13. **MED** — `autoload/SfxManager.gd:197` "Failed to open manifest." → add `MANIFEST_PATH`, `error_string(FileAccess.get_open_error())`.
14. **MED** — `autoload/SaveManager.gd:37,83,91,98,107,115,124,128,142` — every `err=%s` is an int; :98 has no path. → `error_string(err)`; `[SaveManager] slot %d: temp save %s failed read-back validation`.
15. **MED** — `core/settings/SettingsStore.gd:36,40,52,62,94` — no path in any message. → add `_temporary_path` / `_backup_path` / `_primary_path`, `error_string(err)`.
16. **LOW** — `autoload/global.gd:276` "Scene change failed" → `error_string(err)`.
17. **MED** — `core/systems/enemy_world/EnemyProxyRoot.gd:30` "missing autoloads" → name them.
18. **LOW** — `ui/augments/AugmentSelect.gd:92` → `need 3 offers, augment_db has %d`.
19. **LOW** — "scene root is not X" family (`player.gd:696,831,885`, `EnemyDrops.gd:95,230`, `WorldDropSpawner.gd:44`, `InventoryBar.gd:65`, `HudBagController.gd:128`, `SpiderlingSummonEffect.gd:90`) → append `scene=%s`.
20. **LOW** — `scenes/game.gd:189,213,263,268,409,414` → include export name and path; :263 guesses a cause (unassigned export, not a missing file).
21. **LOW** — `SegmentProcBuilder.gd:310` → `seg=%d seed=%d: no primary objective; gate bypassed`.
22. **LOW** — `OpeningSequenceController.gd:56` → say which of player/level failed.
23. **LOW** — `ManifestationRunner.gd:160,261` → add `def.logic.resource_path` and actual class.
24. **LOW** — `WorldDropSpawner.gd:39,44` → add `inst.data.id`.
25. **LOW** — `ItemPickup.gd:156,159` → add `item_id`.
26. **LOW** — `AugmentTooltip.gd:74`, `ItemTooltip.gd:147` "Missing UI nodes" → list which resolved null.
27. **LOW** — `ui/screens/hud.gd:97,99,102` "… may not work." → state the consequence.
28. **LOW / medium** — five wordings of "dir not found" (`global.gd:646,684,844,882`, `MajorChoiceDB.gd:15`) → unify.

### (c) Messages that state the wrong thing

29. **MED** — `AugmentSelect.gd:142` "has no signal 'picked'" sits in the `else` of `if card.has_signal("unhovered")` → names the wrong signal.
30. **MED** — `MagicMissileEffect.gd:62` stale "group must be 'enemies'" (see #2).
31. **LOW** — `SaveSelect.gd:33` "SaveManager autoload not found … until you add one" — it is registered in `project.godot`; the advice describes an impossible state.
32. **LOW** — `HexBlinkMarkEffect.gd:65`, `ReflectShieldEffect.gd:83` "(add augment_active… in Input Map)" — all four actions exist (`project.godot:126,141,146,151`); the real cause would be a bad `active_action` from `AugmentRunner`.
33. **LOW** — `ChunkGenImpl.gd:215,226` carry the prefix `[ChunkManager]` from a different file.

### (d) Duplicated messages for one condition

35. **MED** — InvRouter missing warned in three places (`hud.gd:72`, `HudInventoryController.gd:77`, `WorldDropSpawner.gd:24`) → once.
36. **MED** — ThreatDirector missing: two per-frame twins (#5).
37. **LOW** — augment pick logged three times (`AugmentSelect.gd:172`, `game.gd:297`, `global.gd:787`) → one, debug-gated.
38. **LOW** — `base.gd:96` + `:176` two lines per click.
39. **LOW** — tooltip controllers' identical messages with different prefixes.
40. **LOW** — "Missing UI nodes", "Player not found", Boss/MiniBoss pairs.
41. **LOW** — "item id not found" in five wordings (`EnemyDrops.gd:187`, `ItemPickup.gd:218`, `MCE_GrantItemRoll.gd:17`, `MainMenu.gd:371`, `DevSetCollisionTools.gd:54`) → `[X] unknown item id=%s`.
42. **LOW** — "run_inventory is null" ×3.

### (e) Wrong level

Warning that should be `push_error` (run unrecoverable / soft-locked / silently broken):
43. **HIGH** — `SegmentProcBuilder.gd:80,84` (builder `queue_free()`s itself → no objectives, no exit rite).
44. **HIGH** — `scenes/game.gd:409,414` (tree already `paused = true`, returns without UI → soft-lock).
45. **MED** — `spawner.gd:342`, `ChunkGenImpl.gd:215`, `player.gd:152` (unhittable player), `player.gd:483`, `ManifestationRunner.gd:160,261`, `AugmentSelect.gd:92`.

`push_error` that is arguably a warning: 46. `enemy.gd:210,775` if a pool race (needs a handle in the message first). 47. `SettingsStore.gd` (recoverable; defensible level, but must carry a path).

### (f) `push_error` for expected situations — none found.

### (g) Stale / inconsistent prefixes

48. `DevSetCollisionTools.gd:54,58,116` `"[0.22 set tools]"`, `"[0.22 collision fixture]"` (milestone-numbered); `"[manifestations]"` vs `"[Manifestations]"`.
49. Mixed styles inside one file: `[Spawner]` vs `[SPAWN]`; `AugmentSelect:` vs `[AugmentSelect]`; `[DROP POOL]` vs `DROP ROLL:`.
50. Abbreviations that match no class: `[MM]`, `[HudInv]`, `[HudManifestation]`, `[AUG]`, `[SETS]`.
51. No prefix at all in ~14 files (player, enemy, EnemyDrops, ItemPickup, game, base, SaveSelect, MainMenu, InventoryBar, BossArena, MiniBossArena, EnemyProxyRoot, IndoorVolume, SaveManager, SettingsStore).

### (h) Developer-only prints that ship

52. **HIGH** — `StaminaCoreEffect.gd` ×9; `MagicMissileEffect.gd` ×5 (flag default true).
53. **MED** — `game.gd:118` "Game ready", `:297`, `:406` "GAME OVER"; `base.gd:96,149,176`; `SegmentProcBuilder.gd:114` (dumps the whole validation dictionary per segment ≥2). → `OS.is_debug_build()` at minimum; `debug_proc_state` for :114.
54. **LOW** — 16 prints behind `OS.is_debug_build()` appear in debug exports handed to testers.

### (i) `assert()` without message — none; the codebase uses `push_error + return` consistently.

---

## 2. What a player sees in a release export (normal path)

```
Selected race:Human                                       # base.gd:96      noise, per click
COMMIT -> Global race:human style: weapon:                # base.gd:176     noise, per click
Selected style:Brawler                                    # base.gd:149     noise
COMMIT -> Global race:human style:brawler weapon:...      # base.gd:176     noise
COMMIT -> Global race:human style:brawler weapon:...      # base.gd:378→176 noise (Start Run)
Game ready - segment:1 race:human style:brawler           # game.gd:118     noise
[StaminaCore] ready. has_action=true action=augment_active_1   # if equipped
[MM] ready. missile_scene=<PackedScene#-9223372011234>         # if equipped
[MM] no enemies in radius=750.0 (group must be 'enemies')      # 1/s idle, wrong hint
[MM] FIRE burst at handle:1043                                 # 1–3/s in combat  ← dominates the log
[StaminaCore] pressed. cd=0.0 active_time=0.0             # per keypress
[StaminaCore] activated. cd_max=12.0 duration=4.0
[StaminaCore] aura spawned OK. parent=Hurtbox
[StaminaCore] blocked by cd:9.3
Game ready - segment:2 race:human style:brawler
[SegmentProcBuilder] Theme=Industrial main=9 explore=6 rewards=3 validation={ ... }   # dict dump
AUGMENT RECEIVED IN GAME:aug_stamina_core
GAME OVER
```
Everything above is noise except the `NULL` failure lines, which should become one-shot warnings. No real-session console capture exists in `performance_results/` to cross-check.

---

## 3. Top 15 fixes, ranked by frequency × confusion

| # | Where | Fix |
|---|---|---|
| 1 | `MagicMissileEffect.gd:12,74` | `debug_prints` default `false`; `[MagicMissile] fire handle=%d dmg=%.1f` |
| 2 | `MagicMissileEffect.gd:62` | Drop the stale group hint; keep the 1/s throttle behind the flag |
| 3 | `StaminaCoreEffect.gd:37-116` | Add `debug_prints` export; NULL cases → one-shot `push_warning` with `scene_file_path` |
| 4 | `HudEvacOverlayController.gd:59`, `HudThreatController.gd:59` | Warn once, one place |
| 5 | `SegmentProcBuilder.gd:114` | Gate on `debug_proc_state`; drop the dict dump |
| 6 | `base.gd:96,149,176` | Remove or `OS.is_debug_build()`; one line per commit |
| 7 | `game.gd:118,297,406` | Gate; `[Game] ready seg=%d race=%s style=%s seed=%d` |
| 8 | `spawner.gd:342` | `push_error` once with the table path; disable spawning |
| 9 | `SegmentProcBuilder.gd:80,84`, `game.gd:409,414` | warning → `push_error` |
| 10 | `EnemyDrops.gd:136,182` | Include `spec.id`, prefixes, fallback; once per pool signature |
| 11 | `enemy.gd:210,775` | Add handle/spec/pool_warming; rate-limit |
| 12 | `hud.gd:72`, `HudInventoryController.gd:77`, `WorldDropSpawner.gd:24` | One warning for the InvRouter autoload |
| 13 | `SaveManager.gd` ×9, `SettingsStore.gd` ×5, `global.gd:276` | `error_string(err)`; add paths |
| 14 | `AugmentSelect.gd:142` | Name the signal actually checked |
| 15 | `ChunkGenImpl.gd:215`, `DistrictPlan.gd:773` | One-shot error; aggregate sealed chunks |

---

## 4. Convention to adopt (no framework)

Observed today: `[ClassName] message` (28 files, dominant), `ClassName: message` (6 files), shouting tags, and no prefix (~14 files). The only once-guards in use are `_warned_missing_action` (HexBlink/ReflectShield) and `_printed_once` (ChunkGenImpl) — reuse them.

1. Prefix every line with the emitting script's class name in brackets, a verb phrase, then `key=value` pairs — `[EnemyDrops] no drop: spec=grunt prefixes=[acc_] fallback=false`; never abbreviations, milestone numbers, or ALL-CAPS tags.
2. `push_error` = invariant broken, data loss, or the run/screen cannot continue; `push_warning` = degraded but playable; `print` = developer trace only.
3. Every `print` sits behind a flag that defaults `false` (`@export var debug_prints := false` per effect, or `OS.is_debug_build()` for one-shot lifecycle lines); no unguarded `print` outside `tools/`.
4. Anything reachable from `_process`, a spawn/kill path, or a retry loop warns once per instance via a `_warned_<what>` bool, or aggregates into one line with a count.
5. Always carry the identifier that finds the asset (`spec.id`, `item_id`, `handle=`, `seed=`, `resource_path`, `scene_file_path`) and render engine errors with `error_string(err)`, never `str(err)`.

---

## Status 2026-08-30 — top-15 fixes #1–#3 and finding 44 landed; everything else verified open at `b2b1604`

Read-only re-verification (grep/read at HEAD, nothing executed) plus the git
log since `c131cd2`.

| Fixed | Commit | What changed |
|---|---|---|
| #1 / finding 1 | `2aebf62` | `MagicMissileEffect.gd:12` `debug_prints` now defaults `false`; the burst line is `[MagicMissile] fire handle=` (no `dmg=`/`cd=` — narrower than the suggested format, but gated and prefixed) |
| #2 / findings 2, 30 | `2aebf62` | `:62` is now `[MagicMissile] no enemies in radius=` — stale group hint gone; the 1/s throttle stays, behind the flag |
| #3 / findings 4, 52 | `2aebf62` | StaminaCore gets `@export var debug_prints := false`; the five trace prints are gated; the four aura-spawn failures route through one-shot `_warn_aura_once` → `push_warning` with `scene_file_path`. Deviation: :116 ("aura spawned OK") was kept behind the flag (now `:124`) rather than dropped |
| finding 44 / #9 second half | `3619ddd` | both `end_run()` bail-outs are `push_error` + `_quit_to_menu()` — no more paused-tree soft-lock; the messages now name the export (part of finding 20's ask). `GameOverFallbackTest` covers it |

With that, every `[MM]` prefix is gone (part of finding 50), the
"5 prints behind a default-true flag" class is empty, and the unguarded-print
count drops 16 → 7 (`game.gd:118,297,406`, `base.gd:96,149,176`,
`SegmentProcBuilder.gd:114`).

**Overtaken by deletion:** `ui/controllers/HudInventoryController.gd`
(4 sites) went in `ae86c60` (cleanup win #3), so finding 35 / fix #12 is now a
two-way duplicate — `hud.gd:72` and `WorldDropSpawner.gd:24` both re-verified
present — and finding 50 loses `[HudInv]`. No other file this audit cites was
deleted (the `0428e8d` script deletions all had zero call sites).

**Still open — every other row, re-verified at HEAD:** the ThreatDirector
twins both still warn per frame from `_process` at `:59` (#4/5/36);
`SegmentProcBuilder.gd:114` dict dump and the `:80,84` warn-then-`queue_free`
(#5, 43, #9 first half); `base.gd:96,149,176` and `game.gd:118,297` +
`GAME OVER` unguarded (#6, #7, 53); `spawner.gd:342` per-spawn warning (#8);
`EnemyDrops.gd:136,139,182,187` context-free per-kill warnings (#10);
`enemy.gd` pool-lease errors still carry no handle/spec (#11); `error_string`
still appears nowhere outside the two performance writers (#13);
`AugmentSelect.gd:142` still names the wrong signal (#14, 29);
`ChunkGenImpl.gd:215` still per-chunk under the borrowed `[ChunkManager]`
prefix and `DistrictPlan.gd:773` still one line per chunk in a loop (#15, 33).
Spot checks across (b), (c), (d), (g): findings 13, 17–28, 31, 32, 37–42, 45,
48, 49, 51, 54 all present as written.

**Line drift, same code (stale refs, findings unchanged):** the save/settings
hardening (`3897ef6`, `054f635`, `2dca035`, `9453f95`) grew the audited files
without touching the finding-14/15 pattern — `SaveManager.gd` is now 10
`push_error` at `:45–:174` (the new set-aside error also formats
`err=%s` from `str()`), `SettingsStore.gd` 6 sites at `:42–:107` (the new
newer-schema warning carries versions but still no path), `global.gd:276`→`:277`
plus a new versioned-save warning, and `SaveSelect.gd` gained a third warning
while `:33` still describes its impossible state (finding 31). Also
`enemy.gd:775`→`:767` (`7bfc6cb` removed functions above it),
`ReflectShieldEffect.gd:83`→`:84`, `global.gd:787`→`:788` and
`:646,684,844,882`→`:647,685,845,883`.

Inventory at HEAD: **188 call sites in 50 files** (was 191 in 51) — exactly
−4 StaminaCore prints, −4 HudInventoryController, +1 each
SaveManager / global.gd / SettingsStore / SaveSelect from the save hardening.

No corrections: every re-checked claim held. (`2aebf62`'s message describes
finding 2's targeting as `EnemyCombat.nearest_enemy` where the audit said
"EnemyWorld handle-based" — same point, no group lookup either way. The
numbering gap at 34 predates this status; there is no finding 34.)
